import { useEffect, useMemo, useState } from "react";
import { BarChart3, Bell, Check, ChevronDown, ChevronUp, Copy, Download, FileImage, FileText, Heart, LayoutDashboard, LogIn, LogOut, Menu, MessageSquare, Palette, Pencil, Plus, QrCode, RefreshCw, Save, Search, Settings, ShieldCheck, Trash2, Users, X } from "lucide-react";
import { supabaseAuthPassword, supabaseRest } from "@/lib/supabase";
import { changeAllEventSecurityCodes, changeEventSecurityCode, createAdminEvent, createGuest, createNotification, deleteAdminEvent, deleteGuest, deleteGuestbookMessage, deleteNotification, deleteRsvp, getAdminAnalytics, getAdminEvents, getAdminFeaturedGuestbook, getAdminHomepageSections, getAdminRsvps, getAdminSettings, getAuditLogs, getEventSecurityCodes, getEventSecurityStatus, getGuests, getNotifications, hashEventCode, isSupabaseConfigured, saveSiteSetting, setGuestbookFeatured, setGuestbookModeration, updateAdminEvent, updateGuest, updateHomepageSections, updateRsvp, writeAdminAudit, type EventRecord, type GuestRecord, type HomepageSectionRecord, type NotificationRecord, type RsvpRecord } from "@/lib/supabaseData";

const TOKEN_KEY = "wedding-admin-access-token";
type Tab = "dashboard" | "guests" | "rsvp" | "guestbook" | "gallery" | "events" | "content" | "analytics" | "notifications" | "exports" | "security" | "audit";

function sessionError(error: unknown) { const m = error instanceof Error ? error.message.toLowerCase() : ""; return /jwt|token|unauthorized|401|invalid or expired/.test(m); }
function csvEscape(value: unknown) { const s = value == null ? "" : String(value); return `"${s.replaceAll('"', '""')}"`; }
function downloadCsv(name: string, rows: Record<string, unknown>[]) { if (!rows.length) return; const headers = Object.keys(rows[0]); const csv = [headers.map(csvEscape).join(","), ...rows.map((r) => headers.map((h) => csvEscape(r[h])).join(","))].join("\n"); const blob = new Blob([csv], { type: "text/csv;charset=utf-8" }); const a = document.createElement("a"); a.href = URL.createObjectURL(blob); a.download = name; a.click(); URL.revokeObjectURL(a.href); }
function Card({ children, className = "", id }: { children: React.ReactNode; className?: string; id?: string }) { return <div id={id} className={`rounded-3xl border border-primary/10 bg-white p-5 shadow-sm ${className}`}>{children}</div>; }
function Stat({ label, value, icon: Icon }: { label: string; value: number | string; icon: any }) { return <Card><div className="flex items-start justify-between"><div><p className="text-xs uppercase tracking-widest text-muted-foreground">{label}</p><p className="mt-2 font-display text-3xl">{value}</p></div><div className="grid h-10 w-10 place-items-center rounded-2xl bg-primary/10 text-primary"><Icon size={18}/></div></div></Card>; }

const NAV: { key: Tab; label: string; icon: any }[] = [
  ["dashboard","Dashboard",LayoutDashboard],["guests","Guests",Users],["rsvp","RSVP",Heart],["guestbook","Guestbook",MessageSquare],["gallery","Gallery",FileImage],["events","Events",CalendarIcon],["content","Content",Pencil],["analytics","Analytics",BarChart3],["notifications","Notifications",Bell],["exports","Exports",Download],["security","Security",ShieldCheck],["audit","Audit Log",FileText],
].map(([key,label,icon]) => ({ key: key as Tab, label: label as string, icon }));
function CalendarIcon(props: any) { return <span {...props}>📅</span>; }

export default function Admin() {
  const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [token,setToken]=useState(()=>sessionStorage.getItem(TOKEN_KEY)||""); const [tab,setTab]=useState<Tab>("dashboard"); const [busy,setBusy]=useState(false); const [error,setError]=useState(""); const [sidebar,setSidebar]=useState(false);
  const [events,setEvents]=useState<EventRecord[]>([]); const [rsvps,setRsvps]=useState<RsvpRecord[]>([]); const [guests,setGuests]=useState<GuestRecord[]>([]); const [guestbook,setGuestbook]=useState<any[]>([]); const [analytics,setAnalytics]=useState<any>({total:0,byType:{},recent:[]}); const [settings,setSettings]=useState<any>({}); const [sections,setSections]=useState<HomepageSectionRecord[]>([]); const [notifications,setNotifications]=useState<NotificationRecord[]>([]); const [audit,setAudit]=useState<any[]>([]);
  const [securityCodes,setSecurityCodes]=useState<Record<string,string|null>>({}); const [bulkCode,setBulkCode]=useState(""); const [bulkConfirm,setBulkConfirm]=useState(""); const [bulkMessage,setBulkMessage]=useState(""); const [securityStatus,setSecurityStatus]=useState<any>(null);
  const [search,setSearch]=useState(""); const [rsvpFilter,setRsvpFilter]=useState<"all"|"yes"|"no">("all"); const [guestFilter,setGuestFilter]=useState(""); const [codeInputs,setCodeInputs]=useState<Record<string,string>>({}); const [codeConfirm,setCodeConfirm]=useState<Record<string,string>>({}); const [codeMessage,setCodeMessage]=useState<Record<string,string>>({}); const [newGuest,setNewGuest]=useState({name:"",phone:"",guest_group:"Friends",relationship:"",guest_count:1,notes:""}); const [newEvent,setNewEvent]=useState({slug:"",title:"",date:"",description:"",drive_folder_id:"",code:""});

  const logout=()=>{ if(token) void writeAdminAudit(token,"logout"); sessionStorage.removeItem(TOKEN_KEY); setToken(""); setEvents([]); setRsvps([]); setGuests([]); setGuestbook([]); };
  const load=async(authToken=token)=>{ if(!authToken)return; setBusy(true);setError(""); try { const admin=await supabaseRest<boolean>("rpc/is_admin",{method:"POST",token:authToken,body:{}}); if(!admin)throw new Error("This account is not an admin."); const [ev,rv,gs,gb,an,st,sec,nt,au]=await Promise.all([getAdminEvents(authToken),getAdminRsvps(authToken),getGuests(authToken),getAdminFeaturedGuestbook(authToken),getAdminAnalytics(authToken),getAdminSettings(authToken),getAdminHomepageSections(authToken),getNotifications(authToken),getAuditLogs(authToken)]); setEvents(ev);setRsvps(rv);setGuests(gs);setGuestbook(gb);setAnalytics(an);setSettings(Object.fromEntries(st.map(x=>[x.key,x.value])));setSections(sec);setNotifications(nt);setAudit(au); const codes=await getEventSecurityCodes(authToken); setSecurityCodes(Object.fromEntries(codes.map(c=>[c.id,c.code]))); setSecurityStatus(await getEventSecurityStatus()); } catch(e){ if(sessionError(e)){logout();setError("Your admin session has expired. Please sign in again.");} else setError(e instanceof Error?e.message:"Unable to load admin data."); } finally{setBusy(false);} };
  useEffect(()=>{if(token)void load(token)},[token]);
  useEffect(()=>{ if(!token||!settings.wedding) return; const timer=window.setTimeout(()=>{ try{ localStorage.setItem("wedding-admin-auto-draft",JSON.stringify({savedAt:new Date().toISOString(),wedding:settings.wedding,theme:settings.theme||{},siteControl:settings.siteControl||{}})); }catch{} },1200); return ()=>window.clearTimeout(timer); },[token,settings.wedding,settings.theme,settings.siteControl]);
  const login=async()=>{if(!email.trim()||!password)return setError("Please enter your email and password.");setBusy(true);setError("");try{const s=await supabaseAuthPassword(email.trim(),password);sessionStorage.setItem(TOKEN_KEY,s.access_token);setPassword("");setToken(s.access_token);void writeAdminAudit(s.access_token,"login");}catch(e){setError(e instanceof Error?e.message:"Login failed.")}finally{setBusy(false)}};
  const changeAllCodes=async()=>{ if(bulkCode.length<6||bulkCode.length>128)return setBulkMessage("Use a security code between 6 and 128 characters."); if(bulkCode!==bulkConfirm)return setBulkMessage("The two security codes do not match."); try{setBulkMessage("Changing all event codes…");const result=await changeAllEventSecurityCodes(token,bulkCode);setBulkMessage(result.message);setBulkCode("");setBulkConfirm("");await load();}catch(e){setBulkMessage(e instanceof Error?e.message:"Unable to change all event security codes.")}};

  const act=async(fn:()=>Promise<any>, auditAction?:any)=>{setBusy(true);setError("");try{await fn();if(auditAction)void writeAdminAudit(token,auditAction);await load();}catch(e){if(sessionError(e)){logout();setError("Your admin session has expired.")}else setError(e instanceof Error?e.message:"Action failed.")}finally{setBusy(false)}};

  const stats=useMemo(()=>{const attending=rsvps.filter(r=>r.attending);return {attending:attending.length,notAttending:rsvps.length-attending.length,totalGuests:attending.reduce((n,r)=>n+(r.guest_count||0),0),pendingGuestbook:guestbook.filter(g=>g.moderation_status==="pending").length,approvedGuestbook:guestbook.filter(g=>g.moderation_status==="approved").length,featured:guestbook.filter(g=>g.featured).length,invited:guests.filter(g=>g.invited).length}},[rsvps,guestbook,guests]);
  const filteredRsvps=useMemo(()=>rsvps.filter(r=>(rsvpFilter==="all"||(rsvpFilter==="yes"?r.attending:!r.attending)) && (!search||`${r.name} ${r.email||""} ${r.phone||""}`.toLowerCase().includes(search.toLowerCase()))),[rsvps,rsvpFilter,search]);
  const filteredGuests=useMemo(()=>guests.filter(g=>!guestFilter||`${g.name} ${g.phone||""} ${g.guest_group}`.toLowerCase().includes(guestFilter.toLowerCase())),[guests,guestFilter]);

  const saveEvent=(event:EventRecord)=>act(()=>updateAdminEvent(token,event.id,{title:event.title,date:event.date,description:event.description,drive_folder_id:event.drive_folder_id,venue_name:event.venue_name,venue_address:event.venue_address,maps_url:event.maps_url,is_active:event.is_active,photos_enabled:event.photos_enabled,videos_enabled:event.videos_enabled,sort_order:event.sort_order}),"update_event");
  const changeCode=async(event:EventRecord)=>{const code=codeInputs[event.id]||"";if(code.length<6||code.length>128)return setCodeMessage(x=>({...x,[event.id]:"Use a security code between 6 and 128 characters."}));if(code!==codeConfirm[event.id])return setCodeMessage(x=>({...x,[event.id]:"The two security codes do not match."}));try{setCodeMessage(x=>({...x,[event.id]:"Changing…"}));await changeEventSecurityCode(token,event,code);setCodeInputs(x=>({...x,[event.id]:""}));setCodeConfirm(x=>({...x,[event.id]:""}));setCodeMessage(x=>({...x,[event.id]:"Security code changed successfully."}));await load();}catch(e){setCodeMessage(x=>({...x,[event.id]:e instanceof Error?e.message:"Unable to change the security code."}))}};
  const saveContent=async()=>{await act(async()=>{
    const previous={wedding:settings.wedding||{},theme:settings.theme||{},siteControl:settings.siteControl||{}};
    const history=Array.isArray(settings.siteHistory)?settings.siteHistory:[];
    await saveSiteSetting(token,"wedding",settings.wedding||{});
    await saveSiteSetting(token,"theme",settings.theme||{});
    await saveSiteSetting(token,"siteControl",settings.siteControl||{});
    await saveSiteSetting(token,"siteHistory",[{savedAt:new Date().toISOString(),snapshot:previous},...history].slice(0,20));
  },"update_settings")};
  const setSiteControl=(patch:any)=>setSettings((s:any)=>({...s,siteControl:{...s.siteControl,...patch}}));
  const saveDraft=async()=>{await act(async()=>{await saveSiteSetting(token,"siteDraft",{savedAt:new Date().toISOString(),wedding:settings.wedding||{},theme:settings.theme||{},siteControl:settings.siteControl||{}});},"save_homepage_draft")};
  const publishDraft=async()=>{await act(async()=>{const draft=settings.siteDraft;if(!draft)throw new Error("No saved draft exists.");await saveSiteSetting(token,"wedding",draft.wedding||{});await saveSiteSetting(token,"theme",draft.theme||{});await saveSiteSetting(token,"siteControl",draft.siteControl||{});},"publish_homepage")};
  const previewDraft=()=>window.open(`${window.location.origin}/?preview=draft`,"_blank");

  const setPageEnabled=(page:string,enabled:boolean)=>setSiteControl({pages:{...(settings.siteControl?.pages||{}),[page]:enabled}});
  const sectionEdit=(key:string)=>{const el=document.getElementById(`content-${key}`);el?.scrollIntoView({behavior:"smooth",block:"center"});};
  const saveSections=async()=>act(()=>updateHomepageSections(token,sections),"update_homepage");
  const addGuest=async()=>{if(!newGuest.name.trim())return;await act(()=>createGuest(token,{...newGuest,invited:true}),"create_guest");setNewGuest({name:"",phone:"",guest_group:"Friends",relationship:"",guest_count:1,notes:""})};
  const addEvent=async()=>{if(!newEvent.slug||!newEvent.title||newEvent.code.length<6)return setError("New event needs a slug, title and a security code of at least 6 characters.");const hash=await hashEventCode(newEvent.code);await act(()=>createAdminEvent(token,{slug:newEvent.slug.trim().toLowerCase(),title:newEvent.title.trim(),date:newEvent.date||null,description:newEvent.description,drive_folder_id:newEvent.drive_folder_id,secret_code_hash:hash,sort_order:events.length+1,is_active:true,photos_enabled:true,videos_enabled:true,slideshow_enabled:true,qr_enabled:true}),"update_event");setNewEvent({slug:"",title:"",date:"",description:"",drive_folder_id:"",code:""})};

  if(!isSupabaseConfigured) return <main className="grid min-h-screen place-items-center bg-wedding-cream p-6"><Card><h1 className="font-display text-3xl">Supabase is not configured</h1><p className="mt-2 text-sm text-muted-foreground">Add your VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY to the local environment.</p></Card></main>;
  if(!token) return <main className="grid min-h-screen place-items-center bg-wedding-cream p-5"><Card className="w-full max-w-md"><div className="text-center"><Heart className="mx-auto text-primary" fill="currentColor"/><p className="section-subtitle mt-4">Private Control Center</p><h1 className="font-display text-4xl">Wedding Admin</h1><p className="mt-2 text-sm text-muted-foreground">Manage guests, RSVPs, memories and your wedding website.</p></div><input className="mt-7 h-12 w-full rounded-2xl border px-4" type="email" placeholder="Admin email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="mt-3 h-12 w-full rounded-2xl border px-4" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>{if(e.key==="Enter")void login()}}/><button disabled={busy} onClick={()=>void login()} className="mt-5 flex h-12 w-full items-center justify-center gap-2 rounded-full bg-primary text-primary-foreground disabled:opacity-60">{busy?<RefreshCw className="animate-spin" size={16}/>:<LogIn size={16}/>}Sign in</button>{error&&<p role="alert" className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-600">{error}</p>}</Card></main>;

  const renderTab=()=>{
    if(tab==="dashboard") return <><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat label="Invited guests" value={stats.invited} icon={Users}/><Stat label="RSVPs" value={rsvps.length} icon={Heart}/><Stat label="Attending" value={stats.attending} icon={Check}/><Stat label="Expected guests" value={stats.totalGuests} icon={Users}/><Stat label="Pending wishes" value={stats.pendingGuestbook} icon={MessageSquare}/><Stat label="Approved wishes" value={stats.approvedGuestbook} icon={MessageSquare}/><Stat label="Gallery views" value={analytics.total||0} icon={FileImage}/><Stat label="Photo opens" value={analytics.byType?.photo_open||0} icon={BarChart3}/></div><div className="mt-6 grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><Card><h2 className="font-display text-2xl">Recent activity</h2><div className="mt-4 space-y-3">{audit.slice(0,8).map((a:any)=><div key={a.id} className="flex gap-3 border-b border-border/60 pb-3 text-sm"><span className="text-muted-foreground">{new Date(a.created_at).toLocaleString("en-IN",{day:"2-digit",month:"short",hour:"2-digit",minute:"2-digit"})}</span><span className="font-medium">{String(a.action).replaceAll("_"," ")}</span></div>)}{!audit.length&&<p className="text-sm text-muted-foreground">No activity yet.</p>}</div></Card><Card><h2 className="font-display text-2xl">Quick actions</h2><div className="mt-4 grid gap-2 sm:grid-cols-2 lg:grid-cols-1">{[["guests","Add Guest"],["rsvp","View RSVPs"],["guestbook","Approve Wishes"],["events","Manage Events"],["content","Edit Website"]].map(([t,l])=><button key={t} onClick={()=>setTab(t as Tab)} className="flex items-center justify-between rounded-2xl border border-primary/10 px-4 py-3 text-left text-sm hover:bg-primary/5"><span>{l}</span><ChevronDown className="-rotate-90 text-primary" size={15}/></button>)}</div></Card></div></>;
    if(tab==="rsvp") return <><div className="grid gap-4 md:grid-cols-3"><Stat label="All responses" value={rsvps.length} icon={Heart}/><Stat label="Attending" value={stats.attending} icon={Check}/><Stat label="Total expected guests" value={stats.totalGuests} icon={Users}/></div><Card className="mt-6"><div className="flex flex-wrap gap-2"><div className="relative min-w-[220px] flex-1"><Search className="absolute left-3 top-3 text-muted-foreground" size={16}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search guest, phone or email" className="h-10 w-full rounded-full border pl-9 pr-4 text-sm"/></div>{(["all","yes","no"] as const).map(f=><button key={f} onClick={()=>setRsvpFilter(f)} className={`rounded-full px-4 py-2 text-xs uppercase tracking-wider ${rsvpFilter===f?"bg-primary text-primary-foreground":"border"}`}>{f==="all"?"All":f==="yes"?"Attending":"Not attending"}</button>)}<button onClick={()=>downloadCsv("rsvps.csv",filteredRsvps as any)} className="flex items-center gap-2 rounded-full border px-4 py-2 text-xs"><Download size={14}/>Export CSV</button></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead><tr className="border-b"><th className="p-3">Guest</th><th>Status</th><th>Guests</th><th>Phone</th><th>Submitted</th><th></th></tr></thead><tbody>{filteredRsvps.map(r=><tr key={r.id} className="border-b last:border-0"><td className="p-3"><div className="font-medium">{r.name}</div><div className="text-xs text-muted-foreground">{r.email}</div></td><td>{r.attending?<span className="rounded-full bg-green-50 px-3 py-1 text-xs text-green-700">Yes</span>:<span className="rounded-full bg-gray-100 px-3 py-1 text-xs">No</span>}</td><td>{r.guest_count}</td><td>{r.phone||"—"}</td><td>{new Date(r.created_at).toLocaleDateString("en-IN")}</td><td><button aria-label={`Delete RSVP from ${r.name}`} onClick={()=>act(()=>deleteRsvp(token,r.id),"delete_rsvp")} className="rounded-full p-2 text-red-500 hover:bg-red-50"><Trash2 size={15}/></button></td></tr>)}</tbody></table>{!filteredRsvps.length&&<p className="p-8 text-center text-sm text-muted-foreground">No RSVPs found.</p>}</div></Card></>;
    if(tab==="guests") return <><Card><h2 className="font-display text-2xl">Guest list</h2><div className="mt-4 grid gap-3 md:grid-cols-6"><input value={newGuest.name} onChange={e=>setNewGuest({...newGuest,name:e.target.value})} placeholder="Name" className="h-11 rounded-xl border px-3 md:col-span-2"/><input value={newGuest.phone} onChange={e=>setNewGuest({...newGuest,phone:e.target.value})} placeholder="Phone" className="h-11 rounded-xl border px-3"/><input value={newGuest.guest_group} onChange={e=>setNewGuest({...newGuest,guest_group:e.target.value})} placeholder="Group" className="h-11 rounded-xl border px-3"/><input type="number" min="0" max="20" value={newGuest.guest_count} onChange={e=>setNewGuest({...newGuest,guest_count:Number(e.target.value)})} className="h-11 rounded-xl border px-3"/><button onClick={()=>void addGuest()} className="flex h-11 items-center justify-center gap-2 rounded-xl bg-primary text-sm text-primary-foreground"><Plus size={15}/>Add</button></div><div className="mt-4 flex gap-2"><input value={guestFilter} onChange={e=>setGuestFilter(e.target.value)} placeholder="Search guests" className="h-10 flex-1 rounded-full border px-4 text-sm"/><button onClick={()=>downloadCsv("guests.csv",filteredGuests as any)} className="flex items-center gap-2 rounded-full border px-4 text-xs"><Download size={14}/>Export</button></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead><tr className="border-b"><th className="p-3">Name</th><th>Group</th><th>Phone</th><th>Invited</th><th>Guests</th><th>Notes</th><th></th></tr></thead><tbody>{filteredGuests.map(g=><tr key={g.id} className="border-b"><td className="p-3 font-medium">{g.name}</td><td>{g.guest_group}</td><td>{g.phone||"—"}</td><td>{g.invited?"Yes":"No"}</td><td>{g.guest_count}</td><td>{g.notes||"—"}</td><td><button aria-label={`Delete guest ${g.name}`} onClick={()=>act(()=>deleteGuest(token,g.id),"delete_guest")} className="p-2 text-red-500"><Trash2 size={15}/></button></td></tr>)}</tbody></table></div></Card></>;
    if(tab==="guestbook") return <Card><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-display text-2xl">Guestbook moderation</h2><p className="text-sm text-muted-foreground">Pending {stats.pendingGuestbook} · Approved {stats.approvedGuestbook} · Featured {stats.featured}</p></div><button onClick={()=>downloadCsv("guestbook.csv",guestbook as any)} className="flex items-center gap-2 rounded-full border px-4 py-2 text-xs"><Download size={14}/>Export</button></div><div className="mt-5 grid gap-4 md:grid-cols-2">{guestbook.map(g=><article key={g.id} className="rounded-3xl border border-primary/10 p-5"><div className="flex items-start justify-between gap-3"><div><div className="flex items-center gap-2 text-primary"><Heart size={15} fill="currentColor"/><span className="font-display text-lg">{g.name}</span></div><p className="mt-3 font-accent text-base leading-relaxed">“{g.message}”</p></div><span className="rounded-full bg-muted px-3 py-1 text-[10px] uppercase tracking-wider">{g.moderation_status}</span></div><div className="mt-4 flex flex-wrap gap-2">{g.moderation_status!=="approved"&&<button onClick={()=>act(()=>setGuestbookModeration(token,g.id,"approved"),"approve_guestbook")} className="rounded-full bg-primary px-4 py-2 text-xs text-primary-foreground"><Check size={13} className="mr-1 inline"/>Approve</button>}{g.moderation_status!=="rejected"&&<button onClick={()=>act(()=>setGuestbookModeration(token,g.id,"rejected"),"reject_guestbook")} className="rounded-full border px-4 py-2 text-xs">Reject</button>}<button onClick={()=>act(()=>setGuestbookFeatured(token,g.id,!g.featured),"feature_guestbook")} className={`rounded-full border px-4 py-2 text-xs ${g.featured?"bg-primary/10 text-primary":""}`}>{g.featured?"★ Featured":"☆ Feature wish"}</button><button aria-label="Delete guestbook message" onClick={()=>act(()=>deleteGuestbookMessage(token,g.id),"delete_guestbook")} className="ml-auto rounded-full p-2 text-red-500 hover:bg-red-50"><Trash2 size={15}/></button></div></article>)}{!guestbook.length&&<p className="text-sm text-muted-foreground">No messages yet.</p>}</div></Card>;
    if(tab==="gallery") return <><Card><h2 className="font-display text-2xl">Gallery albums</h2><p className="mt-1 text-sm text-muted-foreground">Each event is an album. Private media remains protected by the existing event lock.</p><div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{events.map(e=>{const url=`${window.location.origin}/gallery?event=${encodeURIComponent(e.slug)}`;const uploadUrl=`${window.location.origin}/upload?event=${encodeURIComponent(e.slug)}`;const qr=`https://api.qrserver.com/v1/create-qr-code/?size=220x220&data=${encodeURIComponent(url)}`;const uploadQr=`https://api.qrserver.com/v1/create-qr-code/?size=220x220&data=${encodeURIComponent(uploadUrl)}`;return <div key={e.id} className="rounded-3xl border p-4"><div className="flex items-start justify-between"><div><h3 className="font-display text-xl">{e.title}</h3><p className="mt-1 text-xs text-muted-foreground">{e.is_active?"Visible":"Hidden"} · {e.photos_enabled?"Photos":"Photos off"}</p></div><a href={qr} target="_blank" rel="noreferrer" aria-label={`Open QR code for ${e.title}`}><QrCode className="text-primary"/></a></div><div className="mt-4 flex gap-2"><button onClick={()=>navigator.clipboard?.writeText(url)} className="flex flex-1 items-center justify-center gap-2 rounded-full border px-3 py-2 text-xs"><Copy size={13}/>Copy gallery link</button><button onClick={()=>navigator.clipboard?.writeText(uploadUrl)} className="flex-1 rounded-full border px-3 py-2 text-xs">Copy upload link</button><a href={url} target="_blank" rel="noreferrer" className="rounded-full bg-primary px-4 py-2 text-xs text-primary-foreground">Open</a><a href={uploadQr} target="_blank" rel="noreferrer" className="rounded-full border px-4 py-2 text-xs">Upload QR</a></div></div>})}</div></Card><Card className="mt-6"><h2 className="font-display text-2xl">Guest photo upload album</h2><p className="mt-2 text-sm text-muted-foreground">The Drive gallery can be extended with a dedicated guest-upload folder. For now, use an event album and QR link; no existing Drive files are moved or deleted by this dashboard.</p></Card></>;
    if(tab==="events") return <><Card><div className="flex items-center justify-between"><div><h2 className="font-display text-2xl">Wedding events</h2><p className="text-sm text-muted-foreground">Edit date, venue, media settings and private access without changing existing security codes.</p></div><button onClick={()=>void load()} className="rounded-full border p-2" aria-label="Refresh events"><RefreshCw size={15}/></button></div><div className="mt-5 grid gap-4 md:grid-cols-2">{events.map(e=><div key={e.id} className="rounded-3xl border p-5"><div className="flex gap-2"><input value={e.title} onChange={x=>setEvents(all=>all.map(v=>v.id===e.id?{...v,title:x.target.value}:v))} className="h-11 flex-1 rounded-xl border px-3 font-display text-lg"/><button onClick={()=>act(()=>deleteAdminEvent(token,e.id),"update_event")} className="rounded-xl p-2 text-red-500 hover:bg-red-50" aria-label={`Delete event ${e.title}`}><Trash2 size={16}/></button></div><div className="mt-3 grid gap-3 sm:grid-cols-2"><input type="date" value={e.date||""} onChange={x=>setEvents(all=>all.map(v=>v.id===e.id?{...v,date:x.target.value}:v))} className="h-11 rounded-xl border px-3 text-sm"/><input value={e.venue_name||""} onChange={x=>setEvents(all=>all.map(v=>v.id===e.id?{...v,venue_name:x.target.value}:v))} placeholder="Venue" className="h-11 rounded-xl border px-3 text-sm"/></div><input value={e.venue_address||""} onChange={x=>setEvents(all=>all.map(v=>v.id===e.id?{...v,venue_address:x.target.value}:v))} placeholder="Venue address" className="mt-3 h-11 w-full rounded-xl border px-3 text-sm"/><input value={e.maps_url||""} onChange={x=>setEvents(all=>all.map(v=>v.id===e.id?{...v,maps_url:x.target.value}:v))} placeholder="Google Maps URL" className="mt-3 h-11 w-full rounded-xl border px-3 text-sm"/><textarea value={e.description} onChange={x=>setEvents(all=>all.map(v=>v.id===e.id?{...v,description:x.target.value}:v))} className="mt-3 min-h-20 w-full rounded-xl border p-3 text-sm"/><input value={e.drive_folder_id} onChange={x=>setEvents(all=>all.map(v=>v.id===e.id?{...v,drive_folder_id:x.target.value}:v))} placeholder="Google Drive folder ID" className="mt-3 h-11 w-full rounded-xl border px-3 text-xs"/><div className="mt-4 flex flex-wrap gap-2"><label className="flex items-center gap-2 text-xs"><input type="checkbox" checked={e.is_active} onChange={x=>setEvents(all=>all.map(v=>v.id===e.id?{...v,is_active:x.target.checked}:v))}/>Visible</label><label className="flex items-center gap-2 text-xs"><input type="checkbox" checked={e.photos_enabled} onChange={x=>setEvents(all=>all.map(v=>v.id===e.id?{...v,photos_enabled:x.target.checked}:v))}/>Photos</label><label className="flex items-center gap-2 text-xs"><input type="checkbox" checked={e.videos_enabled} onChange={x=>setEvents(all=>all.map(v=>v.id===e.id?{...v,videos_enabled:x.target.checked}:v))}/>Videos</label></div><button disabled={busy} onClick={()=>void saveEvent(e)} className="mt-4 flex h-10 items-center gap-2 rounded-full bg-primary px-4 text-xs text-primary-foreground"><Save size={14}/>Save event</button><div className="mt-5 border-t pt-5"><p className="text-sm font-semibold">Private event security code</p><p className="mt-1 text-xs text-muted-foreground">Current code is shown only to administrators. Legacy codes created before this feature cannot be recovered from their one-way hash; change them once to enable secure display.</p><div className="mt-3 grid gap-2 sm:grid-cols-[1fr_1fr_auto]"><div className="mb-3 flex items-center justify-between rounded-xl bg-muted/40 px-3 py-2 text-xs sm:col-span-3"><span>Current code</span><code className="font-mono">{securityCodes[e.id] ? securityCodes[e.id] : "Not recoverable — set a new code"}</code></div><input type="text" value={codeInputs[e.id]||""} onChange={x=>setCodeInputs(v=>({...v,[e.id]:x.target.value}))} placeholder="New code (6+ chars)" className="h-10 rounded-xl border px-3 text-sm"/><input type="password" value={codeConfirm[e.id]||""} onChange={x=>setCodeConfirm(v=>({...v,[e.id]:x.target.value}))} placeholder="Confirm code" className="h-10 rounded-xl border px-3 text-sm"/><button onClick={()=>void changeCode(e)} className="rounded-full bg-primary px-4 text-xs text-primary-foreground">Change</button></div>{codeMessage[e.id]&&<p className={`mt-2 text-xs ${codeMessage[e.id].includes("successfully")?"text-green-600":"text-red-600"}`}>{codeMessage[e.id]}</p>}</div></div>)}</div></Card><Card className="mt-6"><h2 className="font-display text-2xl">Add event / album</h2><div className="mt-4 grid gap-3 md:grid-cols-2"><input value={newEvent.slug} onChange={e=>setNewEvent({...newEvent,slug:e.target.value})} placeholder="Slug e.g. reception" className="h-11 rounded-xl border px-3"/><input value={newEvent.title} onChange={e=>setNewEvent({...newEvent,title:e.target.value})} placeholder="Title" className="h-11 rounded-xl border px-3"/><input type="date" value={newEvent.date} onChange={e=>setNewEvent({...newEvent,date:e.target.value})} className="h-11 rounded-xl border px-3"/><input value={newEvent.drive_folder_id} onChange={e=>setNewEvent({...newEvent,drive_folder_id:e.target.value})} placeholder="Google Drive folder ID" className="h-11 rounded-xl border px-3"/><input type="password" value={newEvent.code} onChange={e=>setNewEvent({...newEvent,code:e.target.value})} placeholder="Initial security code" className="h-11 rounded-xl border px-3"/><textarea value={newEvent.description} onChange={e=>setNewEvent({...newEvent,description:e.target.value})} placeholder="Description" className="min-h-20 rounded-xl border p-3"/></div><button onClick={()=>void addEvent()} className="mt-4 flex items-center gap-2 rounded-full bg-primary px-5 py-2 text-sm text-primary-foreground"><Plus size={15}/>Create event</button></Card></>;
    if(tab==="content") {
      const wedding=settings.wedding||{};
      const control=settings.siteControl||{};
      const setWedding=(key:string,value:any)=>setSettings((s:any)=>({...s,wedding:{...(s.wedding||{}),[key]:value}}));
      const setTheme=(key:string,value:any)=>setSettings((s:any)=>({...s,theme:{...(s.theme||{}),[key]:value}}));
      const Field=({label,help,value,onChange,placeholder,locked=false}:{label:string;help?:string;value:any;onChange:(v:string)=>void;placeholder?:string;locked?:boolean})=><label className="block text-sm"><span className="font-medium">{label}</span>{locked&&<span className="ml-2 text-[10px] uppercase tracking-wider text-muted-foreground">🔒 Locked</span>}{help&&<span className="ml-2 text-[11px] text-muted-foreground">{help}</span>}<input readOnly={locked} value={value??""} onChange={e=>onChange(e.target.value)} placeholder={placeholder} className={`mt-1 h-11 w-full rounded-xl border px-3 ${locked?"bg-muted/40 text-muted-foreground":""}`}/></label>;
      const SaveButton=({label="Save Changes",onClick=()=>saveContent()}:{label?:string;onClick?:()=>void})=><button onClick={()=>void onClick()} disabled={busy} className="flex items-center gap-2 rounded-full bg-primary px-4 py-2 text-xs text-primary-foreground disabled:opacity-60"><Save size={14}/>{label}</button>;
      const Section=({id,title,description,children,defaultOpen=false}:{id:string;title:string;description?:string;children:React.ReactNode;defaultOpen?:boolean})=><details id={`content-${id}`} open={defaultOpen} className="scroll-mt-6 rounded-3xl border border-primary/10 bg-white p-0 shadow-sm"><summary className="flex cursor-pointer list-none items-center justify-between gap-4 p-5 [&::-webkit-details-marker]:hidden"><div><h2 className="font-display text-2xl">{title}</h2>{description&&<p className="mt-1 text-sm text-muted-foreground">{description}</p>}</div><ChevronDown size={18} className="shrink-0 transition-transform"/></summary><div className="border-t border-primary/10 p-5">{children}</div></details>;
      const defaultOrder=["hero","couple","story","gallery","events","rsvp","guestbook","blog","footer"];
      const pageLabels=["gallery","videos","upload","blog"];
      const validation=()=>{
        const errors:string[]=[];
        if(!wedding.groomName?.trim())errors.push("Hero: Groom name is required.");
        if(!wedding.brideName?.trim())errors.push("Hero: Bride name is required.");
        if(!wedding.date)errors.push("Hero: Wedding date is required.");
        if(wedding.mapsUrl){try{new URL(wedding.mapsUrl)}catch{errors.push("Wedding & Venue: Google Maps URL is invalid.")}}
        const youtube=[wedding.blogYoutubeUrl,...(Array.isArray(wedding.blogs)?wedding.blogs.map((b:any)=>b.youtubeUrl):[])].filter(Boolean);
        for(const url of youtube){if(!/youtube\.com|youtu\.be/i.test(String(url)))errors.push(`Blog: invalid YouTube URL: ${url}`)}
        if(wedding.galleryDriveFolderId&&String(wedding.galleryDriveFolderId).includes(" "))errors.push("Gallery: Drive Folder ID cannot contain spaces.");
        return errors;
      };
      const validateAndSave=async()=>{const errors=validation();if(errors.length){setError(errors.join(" "));return;}await saveContent();};
      const checkUrl=(value:string,label:string)=>{if(!value)return setError(`${label}: enter a URL first.`);try{const u=new URL(value);setError(`${label}: ✓ Valid ${u.hostname}`)}catch{setError(`${label}: invalid URL.`)}};
      const checkDrive=(value:string,label:string)=>{if(!value)return setError(`${label}: enter a Drive ID first.`);if(/^[A-Za-z0-9_-]{10,}$/.test(value.trim()))setError(`${label}: ✓ Drive ID format looks valid. Actual access is checked when the media is loaded.`);else setError(`${label}: Drive ID format looks invalid.`)};
      const resetSections=()=>{const byKey=new Map(sections.map(s=>[s.key,s]));setSections(defaultOrder.map((key,i)=>({...byKey.get(key)!,sort_order:i+1})).filter(Boolean));};
      const restoreAutoDraft=()=>{try{const raw=localStorage.getItem("wedding-admin-auto-draft");if(!raw)return setError("No local backup was found.");const draft=JSON.parse(raw);setSettings((s:any)=>({...s,wedding:draft.wedding||s.wedding,theme:draft.theme||s.theme,siteControl:draft.siteControl||s.siteControl}));setError("");}catch{setError("The local backup could not be restored.")}};
      const clearAutoDraft=()=>{localStorage.removeItem("wedding-admin-auto-draft");setError("")};
      const autoDraftExists=typeof window!=="undefined"&&!!localStorage.getItem("wedding-admin-auto-draft");
      const publishValidation=validation();
      return <div className="space-y-5">
        <Card className="border-primary/20 bg-white/95">
          <div className="flex flex-wrap items-center justify-between gap-3"><div><p className="section-subtitle">Home Page Content & Media</p><h2 className="font-display text-3xl">Control Center</h2><p className="mt-1 text-sm text-muted-foreground">All homepage controls are below. Every selection can collapse, preview, edit, hide/show and save independently.</p></div><div className="flex flex-wrap gap-2"><button onClick={()=>window.open(window.location.origin,"_blank")} className="rounded-full border px-4 py-2 text-xs">👁 Preview as Visitor</button><SaveButton label="Save All" onClick={validateAndSave}/></div></div>
          <div className="mt-4 grid gap-3 sm:grid-cols-3"><div className="rounded-2xl border p-3 text-xs"><span className="text-muted-foreground">Published</span><p className="mt-1 font-medium">{settings.siteDraft?.savedAt?"Draft available":"Current settings"}</p></div><div className="rounded-2xl border p-3 text-xs"><span className="text-muted-foreground">Unsaved local backup</span><p className="mt-1 font-medium">{autoDraftExists?"✓ Available":"None"}</p></div><div className="rounded-2xl border p-3 text-xs"><span className="text-muted-foreground">Validation</span><p className={`mt-1 font-medium ${publishValidation.length?"text-amber-600":"text-green-600"}`}>{publishValidation.length?`${publishValidation.length} item(s) to review`:"✓ Ready to publish"}</p></div></div>
          {autoDraftExists&&<div className="mt-4 flex flex-wrap items-center justify-between gap-2 rounded-2xl bg-amber-50 p-3 text-xs text-amber-800"><span>Automatic local backup found from your previous editing session.</span><div className="flex gap-2"><button onClick={restoreAutoDraft} className="rounded-full border border-amber-300 px-3 py-1.5">Restore</button><button onClick={clearAutoDraft} className="rounded-full border border-amber-300 px-3 py-1.5">Clear</button></div></div>}
        </Card>

        <Section id="availability" title="1. Page Availability & Maintenance" description="Control the public website. Admin access always remains available." defaultOpen>
          <div className="grid gap-4 lg:grid-cols-3"><label className="rounded-2xl border p-4 text-sm"><span className="font-medium">Public page mode</span><select value={control.mode||"all"} onChange={e=>setSiteControl({mode:e.target.value})} className="mt-2 h-11 w-full rounded-xl border px-3"><option value="all">All public pages</option><option value="landing">Landing page only</option><option value="disabled">Disable all public pages</option></select><span className="mt-2 block text-xs text-muted-foreground">Use Landing Page Only for a simple entry website.</span></label><label className="flex items-center justify-between rounded-2xl border p-4 text-sm"><span><span className="font-medium block">Maintenance Mode</span><span className="text-xs text-muted-foreground">Shows a maintenance screen to visitors.</span></span><input type="checkbox" checked={!!control.maintenance?.enabled} onChange={e=>setSiteControl({maintenance:{...(control.maintenance||{}),enabled:e.target.checked}})}/></label><div className="rounded-2xl border p-4 text-sm"><p className="font-medium">Current public status</p><p className="mt-2 text-xs text-muted-foreground">{control.maintenance?.enabled?"🔴 Maintenance mode":control.mode==="disabled"?"🔴 Public site disabled":control.mode==="landing"?"🟡 Landing page only":"🟢 All public pages live"}</p><button onClick={()=>window.open(window.location.origin,"_blank")} className="mt-3 rounded-full border px-4 py-2 text-xs">Preview Visitor</button></div></div>
          <div className="mt-4 grid gap-3 md:grid-cols-2"><Field label="Maintenance title" value={control.maintenance?.title||"We'll be back soon"} onChange={v=>setSiteControl({maintenance:{...(control.maintenance||{}),title:v}})}/><Field label="Maintenance button" value={control.maintenance?.buttonText||"Back to Home"} onChange={v=>setSiteControl({maintenance:{...(control.maintenance||{}),buttonText:v}})}/><label className="block text-sm md:col-span-2"><span className="font-medium">Maintenance description</span><textarea value={control.maintenance?.description||"We're preparing something special for you. Please check back shortly."} onChange={e=>setSiteControl({maintenance:{...(control.maintenance||{}),description:e.target.value}})} className="mt-1 min-h-20 w-full rounded-xl border p-3"/></label></div>
          <div className="mt-4 grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{pageLabels.map(page=><label key={page} className="flex items-center justify-between rounded-2xl border p-4 text-sm"><span className="capitalize">{page} page</span><input type="checkbox" checked={control.pages?.[page]!==false} onChange={e=>setPageEnabled(page,e.target.checked)}/></label>)}</div>
          <div className="mt-4 flex justify-end"><SaveButton/></div>
        </Section>

        <Section id="draft" title="2. Draft, Publish & History" description="Edit safely, preview before publishing, and restore previous versions.">
          <div className="flex flex-wrap gap-2"><button onClick={()=>void saveDraft()} className="rounded-full border px-4 py-2 text-xs">💾 Save Draft</button><button onClick={previewDraft} className="rounded-full border px-4 py-2 text-xs">👁 Preview Draft</button><button disabled={!!publishValidation.length||busy} onClick={()=>void publishDraft()} className="rounded-full bg-primary px-4 py-2 text-xs text-primary-foreground disabled:opacity-50">🚀 Publish Draft</button><button onClick={()=>{if(confirm("Discard the saved draft?")){void saveSiteSetting(token,"siteDraft",{}).then(load)}}} className="rounded-full border px-4 py-2 text-xs">Discard Draft</button></div>
          <p className="mt-3 text-xs text-muted-foreground">{settings.siteDraft?.savedAt?`Draft saved ${new Date(settings.siteDraft.savedAt).toLocaleString("en-IN")}`:"No server draft saved yet."}</p>
          {publishValidation.length>0&&<div className="mt-4 rounded-2xl bg-amber-50 p-4 text-xs text-amber-800"><p className="font-medium">Review before publish</p><ul className="mt-2 list-disc pl-5">{publishValidation.map((v:string)=><li key={v}>{v}</li>)}</ul></div>}
          <div className="mt-5 space-y-2">{(settings.siteHistory||[]).slice(0,20).map((h:any,i:number)=><div key={`${h.savedAt}-${i}`} className="flex flex-wrap items-center justify-between gap-2 rounded-2xl border p-3 text-xs"><span>{new Date(h.savedAt).toLocaleString("en-IN")}</span><button onClick={()=>{if(confirm("Restore this version into the current editor?"))setSettings((s:any)=>({...s,wedding:h.snapshot?.wedding||s.wedding,theme:h.snapshot?.theme||s.theme,siteControl:h.snapshot?.siteControl||s.siteControl}))}} className="rounded-full border px-3 py-1.5">Restore</button></div>)}{!(settings.siteHistory||[]).length&&<p className="text-xs text-muted-foreground">No history yet.</p>}</div>
        </Section>

        <Section id="theme" title="3. Theme" description="Keep theme controls safe and structured so the site cannot be broken by custom CSS.">
          <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3"><Field label="Primary theme" value={settings.theme?.primary||"rose"} onChange={v=>setTheme("primary",v)}/><Field label="Heading font" value={settings.theme?.headingFont||"Cormorant Garamond"} onChange={v=>setTheme("headingFont",v)}/><Field label="Body font" value={settings.theme?.bodyFont||"Josefin Sans"} onChange={v=>setTheme("bodyFont",v)}/><label className="flex items-center justify-between rounded-2xl border p-4 text-sm">Animations<input type="checkbox" checked={settings.theme?.animations!==false} onChange={e=>setTheme("animations",e.target.checked)}/></label><label className="flex items-center justify-between rounded-2xl border p-4 text-sm">Dark mode<input type="checkbox" checked={!!settings.theme?.darkMode} onChange={e=>setTheme("darkMode",e.target.checked)}/></label><label className="block text-sm"><span className="font-medium">Theme preset</span><select value={settings.theme?.preset||"elegant"} onChange={e=>setTheme("preset",e.target.value)} className="mt-1 h-11 w-full rounded-xl border px-3"><option value="elegant">Elegant</option><option value="classic">Classic</option><option value="modern">Modern</option><option value="minimal">Minimal</option></select></label></div><div className="mt-4 flex justify-end"><SaveButton/></div>
        </Section>

        <Section id="sections" title="4. Home Page Section Manager" description="View, edit, show/hide and move homepage sections. Dragging and arrow controls are both supported.">
          <div className="space-y-2">{sections.map((section,i)=><div key={section.key} draggable onDragStart={e=>e.dataTransfer.setData("text/plain",section.key)} onDragOver={e=>e.preventDefault()} onDrop={e=>{const from=e.dataTransfer.getData("text/plain");setSections(prev=>{const copy=[...prev];const a=copy.findIndex(x=>x.key===from);if(a<0)return prev;const [item]=copy.splice(a,1);copy.splice(i,0,item);return copy})}} className="flex flex-wrap items-center gap-3 rounded-2xl border bg-background p-3"><span className="cursor-grab text-muted-foreground">☷</span><span className="flex-1 text-sm font-medium">{section.label}</span><button onClick={()=>sectionEdit(section.key)} className="rounded-full border px-3 py-1.5 text-xs">Edit</button><button onClick={()=>window.open(`${window.location.origin}/#${section.key==='hero'?'home':section.key}`,"_blank")} className="rounded-full border px-3 py-1.5 text-xs">View</button><button onClick={()=>setSections(prev=>prev.map(x=>x.key===section.key?{...x,enabled:!x.enabled}:x))} className={`rounded-full px-3 py-1.5 text-[10px] uppercase ${section.enabled?"bg-green-50 text-green-700":"bg-gray-100 text-gray-500"}`}>{section.enabled?"Visible":"Hidden"}</button><button disabled={i===sections.length-1} onClick={()=>setSections(prev=>{if(i===prev.length-1)return prev;const c=[...prev];[c[i],c[i+1]]=[c[i+1],c[i]];return c})} className="rounded-full border p-1.5" aria-label="Move section down"><ChevronDown size={15}/></button><button disabled={i===0} onClick={()=>setSections(prev=>{if(i===0)return prev;const c=[...prev];[c[i-1],c[i]]=[c[i],c[i-1]];return c})} className="rounded-full border p-1.5" aria-label="Move section up"><ChevronUp size={15}/></button></div>)}</div><div className="mt-4 flex flex-wrap justify-end gap-2"><button onClick={resetSections} className="rounded-full border px-4 py-2 text-xs">Reset Default Order</button><SaveButton label="Save Section Layout" onClick={saveSections}/></div>
        </Section>

        <Section id="loading" title="5. Loading Logo & Intro" description="Control the loading screen and intro video experience.">
          <div className="grid gap-4 md:grid-cols-2"><Field label="Loading logo Drive ID" value={wedding.loadingLogoDriveId} onChange={v=>setWedding("loadingLogoDriveId",v)} placeholder="Google Drive file ID"/><Field label="Loading text" value={wedding.loadingText||"Welcome to our wedding"} onChange={v=>setWedding("loadingText",v)}/><label className="block text-sm"><span className="font-medium">Loading duration (ms)</span><input type="number" min="500" max="10000" value={Number(wedding.loadingDuration||1200)} onChange={e=>setWedding("loadingDuration",Number(e.target.value))} className="mt-1 h-11 w-full rounded-xl border px-3"/></label><label className="flex items-center justify-between rounded-2xl border p-4 text-sm">Show intro video<input type="checkbox" checked={wedding.introEnabled!==false} onChange={e=>setWedding("introEnabled",e.target.checked)}/></label><Field label="Intro video Drive ID" value={wedding.introVideoDriveId} onChange={v=>setWedding("introVideoDriveId",v)} placeholder="Google Drive video file ID"/><label className="flex items-center justify-between rounded-2xl border p-4 text-sm">Autoplay intro<input type="checkbox" checked={wedding.introAutoplay!==false} onChange={e=>setWedding("introAutoplay",e.target.checked)}/></label><label className="flex items-center justify-between rounded-2xl border p-4 text-sm">Mute intro<input type="checkbox" checked={wedding.introMuted!==false} onChange={e=>setWedding("introMuted",e.target.checked)}/></label><label className="flex items-center justify-between rounded-2xl border p-4 text-sm">Show skip button<input type="checkbox" checked={wedding.introSkip!==false} onChange={e=>setWedding("introSkip",e.target.checked)}/></label></div><div className="mt-4 flex flex-wrap gap-2">{wedding.loadingLogoDriveId&&<button onClick={()=>checkDrive(wedding.loadingLogoDriveId,"Loading logo")} className="rounded-full border px-3 py-2 text-xs">Check Logo ID</button>}{wedding.introVideoDriveId&&<><button onClick={()=>checkDrive(wedding.introVideoDriveId,"Intro video")} className="rounded-full border px-3 py-2 text-xs">Check Video ID</button><a href={`https://drive.google.com/file/d/${encodeURIComponent(wedding.introVideoDriveId)}/view`} target="_blank" rel="noreferrer" className="rounded-full border px-3 py-2 text-xs text-primary">Open Drive</a></>}</div><div className="mt-4 flex justify-end"><SaveButton/></div>
        </Section>

        <Section id="hero" title="6. Hero" description="The hero is the master source for the couple names and wedding date/time.">
          <div className="grid gap-4 md:grid-cols-2"><Field label="Groom name" value={wedding.groomName} onChange={v=>setWedding("groomName",v)}/><Field label="Bride name" value={wedding.brideName} onChange={v=>setWedding("brideName",v)}/><label className="block text-sm"><span className="font-medium">Wedding date</span><input type="date" value={wedding.date||""} onChange={e=>setWedding("date",e.target.value)} className="mt-1 h-11 w-full rounded-xl border px-3"/></label><Field label="Wedding time" value={wedding.time} onChange={v=>setWedding("time",v)}/><Field label="Timezone" value={wedding.timezone||"Asia/Kolkata"} onChange={v=>setWedding("timezone",v)}/><Field label="Background image Drive ID" value={wedding.heroImageDriveId} onChange={v=>setWedding("heroImageDriveId",v)}/><Field label="Hero heading" value={wedding.heroTitle} onChange={v=>setWedding("heroTitle",v)}/><Field label="Hero subtitle" value={wedding.heroSubtitle} onChange={v=>setWedding("heroSubtitle",v)}/><label className="flex items-center justify-between rounded-2xl border p-4 text-sm">Countdown enabled<input type="checkbox" checked={wedding.countdownEnabled!==false} onChange={e=>setWedding("countdownEnabled",e.target.checked)}/></label></div><div className="mt-4 flex flex-wrap gap-2">{wedding.heroImageDriveId&&<button onClick={()=>checkDrive(wedding.heroImageDriveId,"Hero image")} className="rounded-full border px-3 py-2 text-xs">Check Image ID</button>}</div><div className="mt-4 flex justify-end"><SaveButton/></div>
        </Section>

        <Section id="couple" title="7. Couple" description="Names come from Hero and are locked here. Images can be replaced and positioned independently.">
          <div className="grid gap-4 md:grid-cols-2"><div className="rounded-2xl border p-4"><p className="font-medium">Groom</p><div className="mt-3"><Field label="Name" locked value={wedding.groomName} onChange={()=>{}}/></div><div className="mt-3"><Field label="Image Drive ID" value={wedding.groomImageDriveId} onChange={v=>setWedding("groomImageDriveId",v)}/></div><label className="mt-3 block text-sm"><span className="font-medium">Image position</span><select value={wedding.groomImagePosition||"center"} onChange={e=>setWedding("groomImagePosition",e.target.value)} className="mt-1 h-10 w-full rounded-xl border px-3"><option>center</option><option>top</option><option>bottom</option><option>left</option><option>right</option></select></label><div className="mt-3 flex gap-2"><button onClick={()=>checkDrive(wedding.groomImageDriveId,"Groom image")} className="rounded-full border px-3 py-2 text-xs">Check</button><a href={wedding.groomImageDriveId?`https://drive.google.com/file/d/${encodeURIComponent(wedding.groomImageDriveId)}/view`:"#"} target="_blank" rel="noreferrer" className="rounded-full border px-3 py-2 text-xs">Preview</a></div></div><div className="rounded-2xl border p-4"><p className="font-medium">Bride</p><div className="mt-3"><Field label="Name" locked value={wedding.brideName} onChange={()=>{}}/></div><div className="mt-3"><Field label="Image Drive ID" value={wedding.brideImageDriveId} onChange={v=>setWedding("brideImageDriveId",v)}/></div><label className="mt-3 block text-sm"><span className="font-medium">Image position</span><select value={wedding.brideImagePosition||"center"} onChange={e=>setWedding("brideImagePosition",e.target.value)} className="mt-1 h-10 w-full rounded-xl border px-3"><option>center</option><option>top</option><option>bottom</option><option>left</option><option>right</option></select></label><div className="mt-3 flex gap-2"><button onClick={()=>checkDrive(wedding.brideImageDriveId,"Bride image")} className="rounded-full border px-3 py-2 text-xs">Check</button><a href={wedding.brideImageDriveId?`https://drive.google.com/file/d/${encodeURIComponent(wedding.brideImageDriveId)}/view`:"#"} target="_blank" rel="noreferrer" className="rounded-full border px-3 py-2 text-xs">Preview</a></div></div></div><div className="mt-4 rounded-2xl border bg-muted/20 p-4 text-xs text-muted-foreground">Use the same saved images on desktop and mobile; positioning is stored separately from the file ID. Full homepage preview is available from the Control Center header.</div><div className="mt-4 flex justify-end"><SaveButton/></div>
        </Section>

        <Section id="story" title="8. Story & Memories" description="Story and memory content remains managed in the event dashboard so there is one source of truth."><button onClick={()=>setTab("events")} className="rounded-full border px-4 py-2 text-xs">Open Event Dashboard →</button></Section>

        <Section id="gallery" title="9. Gallery" description="Edit the gallery text and Google Drive folder from here.">
          <div className="grid gap-4 md:grid-cols-2"><Field label="Heading" value={wedding.galleryHeading||"Sweet Memories"} onChange={v=>setWedding("galleryHeading",v)}/><Field label="Description" value={wedding.galleryDescription||"Our Captured Moments"} onChange={v=>setWedding("galleryDescription",v)}/><Field label="Google Drive Folder ID" value={wedding.galleryDriveFolderId} onChange={v=>setWedding("galleryDriveFolderId",v)}/><label className="block text-sm"><span className="font-medium">Image limit</span><input type="number" min="1" max="100" value={Number(wedding.galleryLimit||14)} onChange={e=>setWedding("galleryLimit",Number(e.target.value)||14)} className="mt-1 h-11 w-full rounded-xl border px-3"/></label></div><div className="mt-4 flex flex-wrap gap-2">{wedding.galleryDriveFolderId&&<><button onClick={()=>checkDrive(wedding.galleryDriveFolderId,"Gallery folder")} className="rounded-full border px-3 py-2 text-xs">Check Folder ID</button><a href={`https://drive.google.com/drive/folders/${encodeURIComponent(wedding.galleryDriveFolderId)}`} target="_blank" rel="noreferrer" className="rounded-full border px-3 py-2 text-xs text-primary">Open Drive Folder</a></>}</div><div className="mt-4 flex justify-end"><SaveButton/></div>
        </Section>

        <Section id="events" title="10. Wedding & Venue" description="Wedding date/time is the master value used by the homepage. Timeline items can be added, removed and hidden.">
          <div className="grid gap-4 md:grid-cols-2"><label className="block text-sm"><span className="font-medium">Wedding date</span><input type="date" value={wedding.date||""} onChange={e=>setWedding("date",e.target.value)} className="mt-1 h-11 w-full rounded-xl border px-3"/></label><Field label="Wedding time" value={wedding.time} onChange={v=>setWedding("time",v)}/><Field label="Timezone" value={wedding.timezone||"Asia/Kolkata"} onChange={v=>setWedding("timezone",v)}/><Field label="Venue" value={wedding.venue} onChange={v=>setWedding("venue",v)}/><Field label="Address" value={wedding.address} onChange={v=>setWedding("address",v)}/><Field label="Google Maps URL" value={wedding.mapsUrl} onChange={v=>setWedding("mapsUrl",v)} placeholder="https://maps.google.com/..."/></div><div className="mt-5 rounded-2xl border p-4"><div className="flex flex-wrap items-center justify-between gap-2"><div><p className="font-medium">Timeline</p><p className="text-xs text-muted-foreground">Unlimited timeline items. Use an icon name or custom SVG text.</p></div><button onClick={()=>setWedding("timeline",[...(wedding.timeline||[]),{title:"New Program",time:"",icon:"Heart",visible:true}])} className="rounded-full border px-3 py-2 text-xs"><Plus size={13} className="mr-1 inline"/>Add Timeline</button></div><div className="mt-4 space-y-3">{(wedding.timeline||[]).map((item:any,i:number)=><div key={i} className="rounded-2xl border p-3"><div className="grid gap-2 md:grid-cols-[1fr_140px_160px_auto]"><input value={item.title||""} onChange={e=>setWedding("timeline",(wedding.timeline||[]).map((x:any,j:number)=>j===i?{...x,title:e.target.value}:x))} className="h-10 rounded-xl border px-3 text-sm" placeholder="Title"/><input value={item.time||""} onChange={e=>setWedding("timeline",(wedding.timeline||[]).map((x:any,j:number)=>j===i?{...x,time:e.target.value}:x))} className="h-10 rounded-xl border px-3 text-sm" placeholder="Time"/><input value={item.icon||"Heart"} onChange={e=>setWedding("timeline",(wedding.timeline||[]).map((x:any,j:number)=>j===i?{...x,icon:e.target.value}:x))} className="h-10 rounded-xl border px-3 text-sm" placeholder="Icon / SVG"/><button onClick={()=>setWedding("timeline",(wedding.timeline||[]).filter((_:any,j:number)=>j!==i))} className="rounded-xl border px-3 text-xs text-red-500">Delete</button></div><label className="mt-2 inline-flex items-center gap-2 text-xs"><input type="checkbox" checked={item.visible!==false} onChange={e=>setWedding("timeline",(wedding.timeline||[]).map((x:any,j:number)=>j===i?{...x,visible:e.target.checked}:x))}/>Visible</label></div>)}</div></div><div className="mt-4 flex flex-wrap justify-end gap-2"><button onClick={()=>checkUrl(wedding.mapsUrl,"Google Maps")} className="rounded-full border px-3 py-2 text-xs">Check Maps URL</button><SaveButton/></div>
        </Section>

        <Section id="rsvp" title="11. RSVP" description="Edit the public RSVP heading and description, then manage responses from the RSVP dashboard."><div className="grid gap-4 md:grid-cols-2"><Field label="Heading" value={wedding.rsvpHeading||"RSVP"} onChange={v=>setWedding("rsvpHeading",v)}/><Field label="Description" value={wedding.rsvpDescription||"Your presence would mean the world to us."} onChange={v=>setWedding("rsvpDescription",v)}/></div><div className="mt-4 flex flex-wrap gap-2"><button onClick={()=>setTab("rsvp")} className="rounded-full border px-4 py-2 text-xs">Open RSVP Dashboard →</button><SaveButton/></div></Section>

        <Section id="guestbook" title="12. Guestbook" description="Edit the public Guestbook text. Moderation remains in the Guestbook dashboard."><div className="grid gap-4 md:grid-cols-2"><Field label="Heading" value={wedding.guestbookHeading||"Guestbook"} onChange={v=>setWedding("guestbookHeading",v)}/><Field label="Description" value={wedding.guestbookDescription||"Leave a little love and a message for us to remember."} onChange={v=>setWedding("guestbookDescription",v)}/></div><div className="mt-4 flex flex-wrap gap-2"><button onClick={()=>setTab("guestbook")} className="rounded-full border px-4 py-2 text-xs">Open Guestbook Dashboard →</button><SaveButton/></div></Section>

        <Section id="blog" title="13. Blog" description="Manage YouTube-backed blog entries. Add as many posts as you need.">
          <div className="grid gap-4 md:grid-cols-2"><Field label="YouTube URL" value={wedding.blogYoutubeUrl} onChange={v=>setWedding("blogYoutubeUrl",v)} placeholder="https://www.youtube.com/watch?v=..."/><Field label="Blog title" value={wedding.blogTitle} onChange={v=>setWedding("blogTitle",v)}/><Field label="Author" value={wedding.blogAuthor} onChange={v=>setWedding("blogAuthor",v)}/><Field label="Date" value={wedding.blogDate} onChange={v=>setWedding("blogDate",v)}/><Field label="Blog heading" value={wedding.blogHeading||"Our Latest Wedding News"} onChange={v=>setWedding("blogHeading",v)}/></div><div className="mt-4 flex flex-wrap gap-2">{wedding.blogYoutubeUrl&&<><button onClick={()=>checkUrl(wedding.blogYoutubeUrl,"YouTube")} className="rounded-full border px-3 py-2 text-xs">Check YouTube URL</button><button onClick={()=>window.open(`https://www.youtube.com/watch?v=${encodeURIComponent((wedding.blogYoutubeUrl.match(/[?&]v=([^&]+)/)||[])[1]||"")}`,"_blank")} className="rounded-full border px-3 py-2 text-xs">Open YouTube</button></>}</div><div className="mt-5 flex items-center justify-between gap-2"><p className="font-medium text-sm">Additional Blog Posts</p><button onClick={()=>setWedding("blogs",[...(wedding.blogs||[]),{youtubeUrl:"",title:"",author:"",date:"",visible:true}])} className="rounded-full border px-3 py-2 text-xs"><Plus size={13} className="mr-1 inline"/>Add another Blog</button></div><div className="mt-3 space-y-3">{(wedding.blogs||[]).map((b:any,i:number)=><div key={i} className="rounded-2xl border p-4"><div className="grid gap-2 md:grid-cols-4"><input value={b.youtubeUrl||""} onChange={e=>setWedding("blogs",wedding.blogs.map((x:any,j:number)=>j===i?{...x,youtubeUrl:e.target.value}:x))} placeholder="YouTube URL" className="h-10 rounded-xl border px-3 text-sm"/><input value={b.title||""} onChange={e=>setWedding("blogs",wedding.blogs.map((x:any,j:number)=>j===i?{...x,title:e.target.value}:x))} placeholder="Title" className="h-10 rounded-xl border px-3 text-sm"/><input value={b.author||""} onChange={e=>setWedding("blogs",wedding.blogs.map((x:any,j:number)=>j===i?{...x,author:e.target.value}:x))} placeholder="Author" className="h-10 rounded-xl border px-3 text-sm"/><input value={b.date||""} onChange={e=>setWedding("blogs",wedding.blogs.map((x:any,j:number)=>j===i?{...x,date:e.target.value}:x))} placeholder="Date" className="h-10 rounded-xl border px-3 text-sm"/></div><div className="mt-2 flex items-center justify-between"><label className="inline-flex items-center gap-2 text-xs"><input type="checkbox" checked={b.visible!==false} onChange={e=>setWedding("blogs",wedding.blogs.map((x:any,j:number)=>j===i?{...x,visible:e.target.checked}:x))}/>Visible</label><button onClick={()=>setWedding("blogs",wedding.blogs.filter((_:any,j:number)=>j!==i))} className="rounded-full border px-3 py-1.5 text-xs text-red-500">Delete</button></div></div>)}</div><div className="mt-4 flex justify-end"><SaveButton/></div>
        </Section>

        <Section id="footer" title="14. Footer" description="Edit the footer content and social links from the dashboard."><div className="grid gap-4 md:grid-cols-2"><Field label="Footer text" value={wedding.footerText||"Made with love for our special day"} onChange={v=>setWedding("footerText",v)}/><Field label="Copyright" value={wedding.footerCopyright||""} onChange={v=>setWedding("footerCopyright",v)}/><Field label="Instagram URL" value={wedding.instagramUrl||""} onChange={v=>setWedding("instagramUrl",v)}/><Field label="YouTube URL" value={wedding.youtubeUrl||""} onChange={v=>setWedding("youtubeUrl",v)}/><Field label="Facebook URL" value={wedding.facebookUrl||""} onChange={v=>setWedding("facebookUrl",v)}/><Field label="WhatsApp URL" value={wedding.whatsappUrl||""} onChange={v=>setWedding("whatsappUrl",v)}/></div><div className="mt-4 flex flex-wrap gap-2">{[wedding.instagramUrl,wedding.youtubeUrl,wedding.facebookUrl,wedding.whatsappUrl].filter(Boolean).map((u:string)=><button key={u} onClick={()=>checkUrl(u,"Footer link")} className="rounded-full border px-3 py-2 text-xs">Check {new URL(u).hostname}</button>)}</div><div className="mt-4 flex justify-end"><SaveButton/></div></Section>

        <Card><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="section-subtitle">Site Tools</p><h2 className="font-display text-2xl">Health, Media & Safety</h2><p className="text-sm text-muted-foreground">Quick checks before publishing.</p></div><button onClick={()=>void load()} className="rounded-full border px-4 py-2 text-xs">Refresh Status</button></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><div className="rounded-2xl border p-4 text-sm"><p className="font-medium">Supabase</p><p className="mt-1 text-xs text-green-600">✓ Connected</p></div><div className="rounded-2xl border p-4 text-sm"><p className="font-medium">Event security</p><p className="mt-1 text-xs">{securityStatus?.configured?"✓ Configured":"⚠ Check Security"}</p></div><div className="rounded-2xl border p-4 text-sm"><p className="font-medium">Homepage validation</p><p className="mt-1 text-xs">{publishValidation.length?`⚠ ${publishValidation.length} issue(s)`:"✓ Ready"}</p></div><div className="rounded-2xl border p-4 text-sm"><p className="font-medium">Local backup</p><p className="mt-1 text-xs">{autoDraftExists?"✓ Available":"— None"}</p></div></div><div className="mt-5 rounded-2xl border border-red-200 bg-red-50 p-4"><p className="font-medium text-red-800">Emergency public-site control</p><p className="mt-1 text-xs text-red-700">Disable the public site immediately while keeping Admin available.</p><button onClick={()=>{if(confirm("Disable the public website now? Admin will remain accessible.")){setSiteControl({mode:"disabled"});void saveContent()}}} className="mt-3 rounded-full bg-red-600 px-4 py-2 text-xs text-white">Disable Public Website</button></div></Card>
      </div>;
    }
    if(tab==="analytics") { const recent=analytics.recent||[]; return <><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Stat label="Total events" value={analytics.total||0} icon={BarChart3}/><Stat label="Event views" value={analytics.byType?.event_view||0} icon={FileText}/><Stat label="Photo opens" value={analytics.byType?.photo_open||0} icon={FileImage}/><Stat label="Video opens" value={analytics.byType?.video_open||0} icon={BarChart3}/></div><Card className="mt-6"><h2 className="font-display text-2xl">Top memories</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{(analytics.reactions||[]).map((r:any)=><div key={`${r.event_id}:${r.photo_id}`} className="rounded-2xl border p-4"><p className="font-medium text-sm">{r.photo_id}</p><p className="mt-1 text-xs text-muted-foreground">❤️ {r.heart} · 😍 {r.smile} · total {r.total}</p></div>)}{!(analytics.reactions||[]).length&&<p className="text-sm text-muted-foreground">No photo reactions yet.</p>}</div></Card><Card className="mt-6"><h2 className="font-display text-2xl">Recent analytics</h2><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead><tr className="border-b"><th className="p-3">Time</th><th>Type</th><th>Event</th><th>Media</th></tr></thead><tbody>{recent.map((r:any,i:number)=><tr key={`${r.created_at}-${i}`} className="border-b"><td className="p-3">{new Date(r.created_at).toLocaleString("en-IN")}</td><td>{r.media_type}</td><td>{r.event_id||"—"}</td><td>{r.media_id||"—"}</td></tr>)}</tbody></table></div></Card></> }
    if(tab==="notifications") return <><Card><div className="flex items-center justify-between"><div><h2 className="font-display text-2xl">Notifications</h2><p className="text-sm text-muted-foreground">Admin alerts and wedding-day announcements.</p></div><button onClick={()=>void (async()=>{const title=prompt("Announcement title");if(!title)return;const message=prompt("Message")||"";await act(()=>createNotification(token,{type:"announcement",title,message}),"create_notification")})()} className="flex items-center gap-2 rounded-full bg-primary px-4 py-2 text-xs text-primary-foreground"><Plus size={14}/>New announcement</button></div><div className="mt-5 space-y-3">{notifications.map(n=><div key={n.id} className="flex items-start gap-3 rounded-2xl border p-4"><Bell size={17} className="mt-1 text-primary"/><div className="flex-1"><p className="font-medium">{n.title}</p><p className="mt-1 text-sm text-muted-foreground">{n.message}</p><p className="mt-2 text-[10px] uppercase tracking-wider text-muted-foreground">{new Date(n.created_at).toLocaleString("en-IN")}</p></div><button onClick={()=>act(()=>deleteNotification(token,n.id),"delete_notification")} className="text-red-500" aria-label="Delete notification"><Trash2 size={15}/></button></div>)}{!notifications.length&&<p className="text-sm text-muted-foreground">No notifications.</p>}</div></Card></>;
    if(tab==="exports") return <Card><h2 className="font-display text-2xl">Export center</h2><p className="mt-1 text-sm text-muted-foreground">Download wedding data for planning and records.</p><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{[["Guest List","guests.csv",guests],["RSVP List","rsvps.csv",rsvps],["Guestbook","guestbook.csv",guestbook],["Events","events.csv",events],["Analytics","analytics.csv",analytics.recent||[]],["Audit Log","audit-log.csv",audit]].map(([label,file,data])=><button key={String(file)} onClick={()=>downloadCsv(String(file),data as any)} className="flex items-center gap-3 rounded-2xl border p-4 text-left hover:bg-primary/5"><Download className="text-primary" size={18}/><span><span className="block text-sm font-medium">{label}</span><span className="text-xs text-muted-foreground">Export CSV</span></span></button>)}</div></Card>;
    if(tab==="security") return <><Card><h2 className="font-display text-2xl">Security center</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{[["Admin authentication","Protected by Supabase Auth"],["Database RLS","Enabled on admin tables"],["Event security",securityStatus?.configured?"Configured":"Not configured"],["Rate limiting","Enabled on public APIs"],["Private media","Server-side event cookie"],["Audit logging","Enabled"]].map(([a,b])=><div key={a} className="flex items-center gap-3 rounded-2xl border p-4"><ShieldCheck className={String(b).includes("Not configured")?"text-amber-600":"text-green-600"} size={18}/><div><p className="text-sm font-medium">{a}</p><p className="text-xs text-muted-foreground">{b}</p></div></div>)}</div>{securityStatus&&!securityStatus.configured&&<div className="mt-6 rounded-2xl bg-red-50 p-4 text-sm text-red-700"><strong>Event security is not configured.</strong><p className="mt-1">Missing server variables: {securityStatus.missing.join(", ")}. Add them to .env.local and restart the dev server.</p></div>}<div className="mt-6 rounded-3xl border p-5"><h3 className="font-display text-xl">Change all event codes</h3><p className="mt-1 text-sm text-muted-foreground">Set one code for every active event at once. This is separate from changing an individual event.</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><input type="text" value={bulkCode} onChange={e=>setBulkCode(e.target.value)} placeholder="New common code" className="h-11 rounded-xl border px-3"/><input type="text" value={bulkConfirm} onChange={e=>setBulkConfirm(e.target.value)} placeholder="Confirm common code" className="h-11 rounded-xl border px-3"/></div><button onClick={()=>void changeAllCodes()} className="mt-4 rounded-full bg-primary px-5 py-2.5 text-sm text-primary-foreground">Change all event codes</button>{bulkMessage&&<p className={`mt-2 text-xs ${bulkMessage.includes("successfully")||bulkMessage.includes("changed")?"text-green-600":"text-red-600"}`}>{bulkMessage}</p>}</div></Card></>;
    if(tab==="audit") return <Card><div className="flex items-center justify-between"><div><h2 className="font-display text-2xl">Audit log</h2><p className="text-sm text-muted-foreground">Administrative actions are recorded with timestamp and target.</p></div><button onClick={()=>downloadCsv("audit-log.csv",audit)} className="flex items-center gap-2 rounded-full border px-4 py-2 text-xs"><Download size={14}/>Export</button></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead><tr className="border-b"><th className="p-3">Time</th><th>Action</th><th>Target</th><th>Details</th></tr></thead><tbody>{audit.map(a=><tr key={a.id} className="border-b"><td className="p-3">{new Date(a.created_at).toLocaleString("en-IN")}</td><td>{String(a.action).replaceAll("_"," ")}</td><td>{a.target_id||"—"}</td><td className="max-w-[300px] truncate">{JSON.stringify(a.details||{})}</td></tr>)}</tbody></table></div></Card>;
    return null;
  };

  return <div className="min-h-screen bg-wedding-cream md:flex"><aside className={`fixed inset-y-0 left-0 z-50 w-72 transform border-r bg-background p-5 transition-transform md:sticky md:top-0 md:block md:h-screen md:translate-x-0 ${sidebar?"translate-x-0":"-translate-x-full"}`}><div className="flex items-center justify-between"><div><p className="font-display text-xl">H & P</p><p className="text-[10px] uppercase tracking-widest text-muted-foreground">Wedding Admin</p></div><button className="md:hidden" onClick={()=>setSidebar(false)} aria-label="Close admin menu"><X size={18}/></button></div><nav className="mt-7 space-y-1">{NAV.map(({key,label,icon:Icon})=><button key={key} onClick={()=>{setTab(key);setSidebar(false)}} className={`flex w-full items-center gap-3 rounded-2xl px-3 py-2.5 text-left text-sm ${tab===key?"bg-primary text-primary-foreground":"text-muted-foreground hover:bg-primary/5 hover:text-foreground"}`}><Icon size={16}/>{label}{key==="guestbook"&&stats.pendingGuestbook>0&&<span className="ml-auto rounded-full bg-red-500 px-2 py-0.5 text-[10px] text-white">{stats.pendingGuestbook}</span>}</button>)}</nav><div className="mt-7 border-t pt-4"><button onClick={logout} className="flex w-full items-center gap-3 rounded-2xl px-3 py-2.5 text-sm text-muted-foreground hover:bg-red-50 hover:text-red-600"><LogOut size={16}/>Sign out</button></div></aside><button className="fixed left-4 top-4 z-40 rounded-full border bg-background p-3 shadow md:hidden" onClick={()=>setSidebar(true)} aria-label="Open admin menu"><Menu size={18}/></button><main className="min-w-0 flex-1 p-4 pt-20 sm:p-6 md:p-8 md:pt-8"><div className="mx-auto max-w-7xl"><header className="flex flex-wrap items-center justify-between gap-4"><div><p className="section-subtitle">Control Center</p><h1 className="font-display text-4xl">{NAV.find(n=>n.key===tab)?.label||"Dashboard"}</h1></div><div className="flex items-center gap-2"><span className="hidden text-xs text-muted-foreground sm:block">{new Date().toLocaleDateString("en-IN",{weekday:"short",day:"2-digit",month:"short"})}</span><button disabled={busy} onClick={()=>void load()} className="flex h-10 items-center gap-2 rounded-full border px-4 text-sm disabled:opacity-60"><RefreshCw size={15} className={busy?"animate-spin":""}/>Refresh</button></div></header>{error&&<div className="mt-5 flex items-start justify-between rounded-2xl bg-red-50 p-4 text-sm text-red-600"><span>{error}</span><button onClick={()=>setError("")} aria-label="Dismiss error"><X size={16}/></button></div>}<section className="mt-7">{renderTab()}</section></div></main></div>;
}
