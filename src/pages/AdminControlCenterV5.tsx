import { useEffect, useMemo, useState, type ReactNode } from "react";
import { Activity, ArrowDown, ArrowUp, BarChart3, Bell, Check, ChevronRight, Download, Eye, EyeOff, ExternalLink, FileArchive, Heart, KeyRound, LogOut, Maximize2, Menu, Minimize2, Moon, Plus, QrCode, RefreshCw, Rocket, Search, ShieldCheck, Sparkles, Sun, Trash2, UserCog, X } from "lucide-react";
import { ADMIN_MODULES, GROUP_LABELS } from "@/admin/moduleRegistry";
import { isSupabaseConfigured, supabaseRest } from "@/lib/supabase";
import { getAdminEvents, getAdminFeaturedGuestbook, getNotifications, getAuditLogs, getAdminSettings, getAdminAnalytics, getAdminHomepageSections, updateHomepageSections, saveSiteSetting, writeAdminAudit, setGuestbookModeration, setGuestbookFeatured, deleteGuestbookMessage, createNotification, deleteNotification, getEventSecurityCodes, changeEventSecurityCode, changeAllEventSecurityCodes, type EventRecord } from "@/lib/supabaseData";
import WebsiteContentPanel from "./WebsiteContentPanel";

type R = Record<string, any>;
type Health = { label: string; status: "ok" | "warn" | "error"; detail: string };
const TOKEN_KEY = "wedding-admin-access-token";
const REFRESH_KEY = "wedding-admin-refresh-token";
const PREVIEW_KEY = "wedding-admin-preview-snapshot-v1";
const SECTION_TO_CHILD: Record<string, string> = { hero: "home", couple: "couple", story: "story", gallery: "gallery", events: "events", rsvp: "rsvp", guestbook: "guestbook", footer: "footer" };
const title = (x: string) => x.replaceAll("-", " ").replace(/\b\w/g, c => c.toUpperCase());
const driveUrl = (value?: string) => { const v = String(value || "").trim(); if (!v) return ""; return /^https?:\/\//i.test(v) ? v : `https://drive.google.com/drive/folders/${encodeURIComponent(v)}`; };
function Btn({ children, onClick, primary = false, danger = false, disabled = false }: { children: ReactNode; onClick?: () => void; primary?: boolean; danger?: boolean; disabled?: boolean }) { return <button type="button" disabled={disabled} onClick={onClick} className={`inline-flex min-h-10 items-center justify-center gap-2 rounded-xl border px-3 text-sm font-medium hover:bg-muted disabled:opacity-50 ${primary ? "border-primary bg-primary text-primary-foreground" : danger ? "border-red-200 text-red-700" : "bg-background"}`}>{children}</button>; }
function Input({ label, value, onChange, type = "text", placeholder = "", help }: { label: string; value: any; onChange: (v: string) => void; type?: string; placeholder?: string; help?: string }) { return <label className="block text-sm font-medium"><span>{label}</span><input type={type} value={value ?? ""} placeholder={placeholder} onChange={e => onChange(e.target.value)} className="mt-1 h-11 w-full rounded-xl border bg-background px-3 outline-none focus:ring-2 focus:ring-primary/20" />{help && <span className="mt-1 block text-xs text-muted-foreground">{help}</span>}</label>; }
function Area({ label, value, onChange }: { label: string; value: any; onChange: (v: string) => void }) { return <label className="block text-sm font-medium"><span>{label}</span><textarea value={value ?? ""} onChange={e => onChange(e.target.value)} className="mt-1 min-h-28 w-full rounded-xl border bg-background p-3" /></label>; }
function Toggle({ label, value, onChange }: { label: string; value: any; onChange: (v: boolean) => void }) { return <label className="flex items-center justify-between gap-4 rounded-xl border p-3 text-sm"><span>{label}</span><input type="checkbox" checked={Boolean(value)} onChange={e => onChange(e.target.checked)} /></label>; }
function Card({ title, children, action }: { title: string; children: ReactNode; action?: ReactNode }) { return <section className="rounded-3xl border bg-card p-5 shadow-sm md:p-6"><div className="mb-4 flex items-center justify-between gap-3"><h2 className="font-semibold">{title}</h2>{action}</div>{children}</section>; }
function Empty({ text }: { text: string }) { return <div className="rounded-2xl border border-dashed p-8 text-center text-sm text-muted-foreground">{text}</div>; }

export default function AdminControlCenterV5() {
 const [token,setToken]=useState(()=>sessionStorage.getItem(TOKEN_KEY)||""); const [active,setActive]=useState("dashboard"); const [child,setChild]=useState(""); const [search,setSearch]=useState(""); const [mobile,setMobile]=useState(false); const [dark,setDark]=useState(()=>localStorage.getItem("admin-theme")==="dark"); const [busy,setBusy]=useState(false); const [toast,setToast]=useState(""); const [fullscreen,setFullscreen]=useState(false); const [events,setEvents]=useState<EventRecord[]>([]); const [guestbook,setGuestbook]=useState<R[]>([]); const [notifications,setNotifications]=useState<R[]>([]); const [audit,setAudit]=useState<R[]>([]); const [settings,setSettings]=useState<R>({}); const [analytics,setAnalytics]=useState<R>({}); const [sections,setSections]=useState<R[]>([]); const [health,setHealth]=useState<Health[]>([]);
 const modules=useMemo(()=>{const q=search.trim().toLowerCase();return q?ADMIN_MODULES.filter(m=>`${m.label} ${m.description} ${m.children.map(c=>c.label).join(" ")}`.toLowerCase().includes(q)):ADMIN_MODULES},[search]); const current=ADMIN_MODULES.find(m=>m.id===active)||ADMIN_MODULES[0];
 const websiteChildren=useMemo(()=>{if(active!=="website")return current.children;const enabled=sections.filter(s=>Boolean(s.enabled)&&SECTION_TO_CHILD[s.key]).sort((a,b)=>Number(a.sort_order||0)-Number(b.sort_order||0)).map(s=>({id:SECTION_TO_CHILD[s.key],label:s.label||title(SECTION_TO_CHILD[s.key])}));return [{id:"section-manager",label:"section manager"},...enabled,{id:"navigation",label:"navigation"}].filter((x,i,a)=>a.findIndex(y=>y.id===x.id)===i)},[active,current.children,sections]);
 useEffect(()=>{document.documentElement.classList.toggle("dark",dark);localStorage.setItem("admin-theme",dark?"dark":"light")},[dark]);
 useEffect(()=>{const sync=()=>setFullscreen(Boolean(document.fullscreenElement));document.addEventListener("fullscreenchange",sync);sync();return()=>document.removeEventListener("fullscreenchange",sync)},[]);
 useEffect(()=>{if(active!=="website"){setChild(current.children[0]?.id||"");return;}if(child==="section-manager")return;if(!websiteChildren.some(c=>c.id===child))setChild(websiteChildren[0]?.id||"section-manager")},[active,current.children,websiteChildren,child]);
 const notify=(x:string)=>{setToast(x);window.setTimeout(()=>setToast(""),3000)};
 const load=async()=>{if(!token)return;setBusy(true);const h:Health[]=[{label:"Supabase",status:isSupabaseConfigured?"ok":"error",detail:isSupabaseConfigured?"Configured":"Missing configuration"}];try{const ok=await supabaseRest<boolean>("rpc/is_admin",{method:"POST",token,body:{}});h.push({label:"Admin authorization",status:ok?"ok":"error",detail:ok?"Verified":"Denied"});if(!ok){sessionStorage.clear();setToken("");setBusy(false);return}}catch(e){h.push({label:"Admin authorization",status:"error",detail:e instanceof Error?e.message:"Failed"})}try{const [ev,gb,no,al,st,an,hs]=await Promise.all([getAdminEvents(token),getAdminFeaturedGuestbook(token),getNotifications(token),getAuditLogs(token),getAdminSettings(token),getAdminAnalytics(token),getAdminHomepageSections(token)]);setEvents(ev||[]);setGuestbook(gb||[]);setNotifications(no||[]);setAudit(al||[]);setSettings(Object.fromEntries((st||[]).map(x=>[x.key,x.value])));setAnalytics(an||{});setSections(hs||[]);h.push({label:"Admin data",status:"ok",detail:"Loaded"})}catch(e){h.push({label:"Admin data",status:"warn",detail:e instanceof Error?e.message:"Some data unavailable"})}try{const r=await fetch("/api/admin-users?action=list_sessions",{headers:{Authorization:`Bearer ${token}`}});h.push({label:"Session service",status:r.ok?"ok":"warn",detail:r.ok?"Reachable":"Unavailable"})}catch{h.push({label:"Session service",status:"warn",detail:"Unavailable"})}setHealth(h);setBusy(false)}; useEffect(()=>{void load()},[token]);
 const go=(id:string)=>{setActive(id);setMobile(false);window.scrollTo({top:0,behavior:"smooth"})}; const logout=()=>{sessionStorage.removeItem(TOKEN_KEY);sessionStorage.removeItem(REFRESH_KEY);setToken("")};
 const toggleFullscreen=async()=>{try{if(document.fullscreenElement)await document.exitFullscreen();else await document.documentElement.requestFullscreen()}catch(e){notify(e instanceof Error?e.message:"Fullscreen is unavailable in this browser.")}};
 const preview=async()=>{try{const response=await fetch("/api/admin-advanced?action=draft",{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({label:`Preview draft ${new Date().toLocaleString()}`})});const data=await response.json().catch(()=>({}));if(!response.ok||!data.ok)throw new Error(data.error||"Unable to create preview draft.");localStorage.setItem(PREVIEW_KEY,JSON.stringify({revision_id:data.revision.id,snapshot:data.revision.snapshot,created_at:new Date().toISOString()}));window.open("/?preview=draft","_blank","noopener,noreferrer");notify("Preview opened with the saved draft.")}catch(e){notify(e instanceof Error?e.message:"Preview failed")}};
 const publish=async()=>{try{setBusy(true);const response=await fetch("/api/admin-advanced?action=publish",{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({label:`Publish ${new Date().toLocaleString()}`})});const data=await response.json().catch(()=>({}));if(!response.ok||!data.ok)throw new Error(data.error||"Publish failed.");await writeAdminAudit(token,"publish_homepage",String(data.revision_id),{version_no:data.version_no});notify(`Published Version ${data.version_no}.`);await load()}catch(e){notify(e instanceof Error?e.message:"Publish failed")}finally{setBusy(false)}};
 if(!token)return <div className="grid min-h-screen place-items-center p-6"><div className="rounded-3xl border bg-card p-8 text-center"><Sparkles className="mx-auto mb-3"/><h1 className="text-2xl font-semibold">Admin session required</h1></div></div>;
 return <div className="min-h-screen bg-muted/20"><header className="sticky top-0 z-50 border-b bg-background/95 backdrop-blur"><div className="flex h-16 items-center gap-2 px-3 md:px-5"><Btn onClick={()=>setMobile(true)}><Menu size={18}/></Btn><button type="button" onClick={()=>go("dashboard")} className="flex items-center gap-2 font-semibold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-primary text-primary-foreground"><Sparkles size={17}/></span><span className="hidden sm:block">Wedding Admin</span></button><div className="relative ml-auto hidden max-w-md flex-1 md:block"><Search className="absolute left-3 top-1/2 -translate-y-1/2" size={16}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search modules…" className="h-10 w-full rounded-xl border pl-9"/></div><button type="button" onClick={()=>void toggleFullscreen()} aria-label={fullscreen?"Exit fullscreen":"Enter fullscreen"} title={fullscreen?"Exit fullscreen":"Enter fullscreen"} className="inline-flex h-10 w-10 shrink-0 items-center justify-center rounded-xl border bg-background hover:bg-muted">{fullscreen?<Minimize2 size={17}/>:<Maximize2 size={17}/>}</button><Btn onClick={()=>void preview()} disabled={busy}><Eye size={16}/><span className="hidden sm:inline">Preview</span></Btn><Btn primary onClick={()=>void publish()} disabled={busy}><Rocket size={16}/><span className="hidden sm:inline">{busy?"Publishing…":"Publish"}</span></Btn><Btn onClick={()=>setDark(v=>!v)}>{dark?<Sun size={17}/>:<Moon size={17}/>}</Btn><Btn onClick={logout}><LogOut size={16}/></Btn></div></header><div className="flex"><aside className="sticky top-16 hidden h-[calc(100vh-4rem)] w-64 shrink-0 overflow-y-auto border-r bg-background p-3 md:block">{(["wedding","insights","management","system"] as const).map(g=><div key={g} className="mb-5"><div className="px-2 py-2 text-[10px] font-semibold tracking-widest text-muted-foreground">{GROUP_LABELS[g]}</div>{modules.filter(m=>m.group===g).map(m=><button key={m.id} type="button" onClick={()=>go(m.id)} className={`mb-1 flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm ${active===m.id?"bg-primary text-primary-foreground":"hover:bg-muted"}`}><m.icon size={17}/>{m.label}</button>)}</div>)}</aside>{mobile&&<div className="fixed inset-0 z-[70] md:hidden"><button className="absolute inset-0 bg-black/40" onClick={()=>setMobile(false)}/><aside className="relative h-full w-[88vw] overflow-y-auto bg-background p-4"><div className="mb-4 flex justify-between"><b>Administration</b><Btn onClick={()=>setMobile(false)}><X size={17}/></Btn></div>{modules.map(m=><button key={m.id} type="button" onClick={()=>go(m.id)} className="mb-1 flex w-full gap-3 rounded-xl px-3 py-2.5 text-left hover:bg-muted"><m.icon size={17}/>{m.label}</button>)}</aside></div>}
 <main className="min-w-0 flex-1 p-4 md:p-7"><div className="mx-auto max-w-[1550px]"><div className="mb-6 flex items-end justify-between gap-3"><div><div className="text-xs text-muted-foreground">Admin <ChevronRight size={12} className="inline"/> {current.label}</div><h1 className="text-3xl font-semibold">{current.label}</h1><p className="mt-1 text-sm text-muted-foreground">{current.description}</p></div><div className="flex gap-2"><span className="hidden items-center rounded-full border bg-background px-3 py-2 text-xs text-muted-foreground sm:inline-flex">Draft workspace · Save does not publish</span><Btn onClick={()=>void load()} disabled={busy}><RefreshCw className={busy?"animate-spin":""} size={15}/><span className="hidden sm:inline">Refresh</span></Btn></div></div>{active==="dashboard"?<Dashboard {...{events,guestbook,analytics,health,go}}/>:<ModuleView {...{module:current,child,setChild,token,settings,setSettings,events,setEvents,guestbook,setGuestbook,notifications,setNotifications,analytics,audit,health,sections,setSections,notify,websiteChildren}}/>}</div></main></div>{toast&&<div className="admin-feedback-toast fixed bottom-5 right-5 z-[100] rounded-2xl border bg-background p-4 text-sm shadow-xl" role="status" aria-live="polite">{toast}</div>}</div>;
}
function Dashboard({events,guestbook,analytics,health,go}:any){return <div className="space-y-6"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-5">{[["Events",events.length,"events"],["Guestbook",guestbook.length,"guestbook"],["Visitors",analytics.total||0,"analytics"],["Reactions",analytics.reactionTotal||0,"interactions"],["Health",`${health.filter((h:Health)=>h.status==="ok").length}/${health.length}`,"system"]].map(([a,b,id])=><button key={String(a)} type="button" onClick={()=>go(id)} className="rounded-2xl border bg-card p-5 text-left"><div className="text-xs text-muted-foreground">{a}</div><div className="mt-2 text-3xl font-semibold">{b}</div></button>)}</div><Card title="Quick actions"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{["website","appearance","loading-intro","events","media","guestbook","interactions","social","locations","qr","analytics","security","administration","backup","publishing","diagnostics"].map(id=><Btn key={id} onClick={()=>go(id)}>{title(id)}<ChevronRight size={15}/></Btn>)}</div></Card><Card title="System health"><div className="grid gap-3 md:grid-cols-2">{health.map(h=><div key={h.label} className="rounded-xl border p-3"><b>{h.label}</b><div className="text-xs text-muted-foreground">{h.detail}</div></div>)}</div></Card></div>}
function ModuleView(p:any){const {module,child,setChild,sections}=p;const website=module.id==="website";const selected=website&&p.websiteChildren.some((c:any)=>c.id===child)?child:"section-manager";const websiteContent=website&&["home","couple","story","gallery","rsvp","footer","navigation"].includes(selected);const websiteEvents=website&&selected==="events";const websiteGuestbook=website&&selected==="guestbook";const tabs=website?p.websiteChildren:module.children;return <div className="space-y-5"><div className="flex gap-2 overflow-x-auto">{tabs.map((c:any)=><button key={c.id} type="button" onClick={()=>setChild(c.id)} className={`whitespace-nowrap rounded-xl border px-3 py-2 text-sm ${((website?selected:child)===c.id)?"bg-primary text-primary-foreground":"bg-background"}`}>{title(c.id)}</button>)}</div>{websiteContent?<WebsiteContentPanel {...p}/>:website&&selected==="section-manager"?<Homepage {...p}/>:websiteEvents?<Events {...p}/>:websiteGuestbook?<Guestbook {...p}/>:module.id==="events"?<Events {...p}/>:module.id==="guestbook"?<Guestbook {...p}/>:module.id==="notifications"?<Notifications {...p}/>:module.id==="analytics"?<Analytics {...p}/>:module.id==="audit"?<Audit {...p}/>:module.id==="security"?<Security {...p}/>:module.id==="administration"?<Administration {...p}/>:module.id==="backup"?<Backup {...p}/>:module.id==="qr"?<QR {...p}/>:module.id==="locations"?<Locations {...p}/>:module.id==="publishing"?<Publishing {...p}/>:module.id==="diagnostics"?<Diagnostics {...p}/>:module.id==="system"?<System {...p}/>:<Generic {...p}/>}</div>}
function Homepage({token,sections,setSections,setChild,notify}:any){const [rows,setRows]=useState(sections);useEffect(()=>setRows(sections),[sections]);const move=(i:number,d:number)=>{const n=[...rows],j=i+d;if(j<0||j>=n.length)return;[n[i],n[j]]=[n[j],n[i]];setRows(n)};const save=async()=>{try{const normalized=rows.map((r:any,i:number)=>({...r,sort_order:i+1,enabled:Boolean(r.enabled)}));await updateHomepageSections(token,normalized);setSections(normalized);notify("Website layout saved to draft.")}catch(e){notify(e instanceof Error?e.message:"Save failed")}};return <Card title="Section Manager" action={<Btn primary onClick={()=>void save()}><Check size={15}/><span className="hidden sm:inline">Save Draft</span></Btn>}><div className="mb-4 rounded-2xl border border-dashed p-4 text-sm text-muted-foreground">This is the master website layout. Enable a section to show its editor in Website. Reorder sections here to control the public website order. Save updates the draft workspace only.</div><div className="space-y-2">{rows.map((s:any,i:number)=><div key={s.key} className="flex flex-wrap items-center gap-2 rounded-xl border p-3"><div className="min-w-0 flex-1"><b>{s.label}</b><div className="text-xs text-muted-foreground">{s.key}</div></div><div className="flex shrink-0 items-center gap-1 rounded-xl border p-1"><button type="button" onClick={()=>setRows((x:any[])=>x.map((a,k)=>k===i?{...a,enabled:!a.enabled}:a))} className="inline-flex h-9 w-9 items-center justify-center rounded-lg hover:bg-muted" aria-label={s.enabled?"Hide section":"Show section"} title={s.enabled?"Hide section":"Show section"}>{s.enabled?<Eye size={16}/>:<EyeOff size={16}/>}</button><button type="button" onClick={()=>{const target=SECTION_TO_CHILD[s.key];if(target){setChild(target);window.scrollTo({top:0,behavior:"smooth"})}}} disabled={!SECTION_TO_CHILD[s.key]} className="inline-flex h-9 w-9 items-center justify-center rounded-lg hover:bg-muted disabled:opacity-50" aria-label="Edit section" title="Edit section"><Check size={16}/></button><button type="button" onClick={()=>window.open(`/?section=${encodeURIComponent(s.key)}`,"_blank","noopener,noreferrer")} className="inline-flex h-9 w-9 items-center justify-center rounded-lg hover:bg-muted" aria-label="View section" title="View section"><ExternalLink size={16}/></button></div><div className="flex shrink-0 gap-1"><Btn onClick={()=>move(i,-1)} disabled={!i}><ArrowUp size={15}/></Btn><Btn onClick={()=>move(i,1)} disabled={i===rows.length-1}><ArrowDown size={15}/></Btn></div></div>)}</div></Card>}
function Events({token,events,setEvents,child,notify}:any){const [selected,setSelected]=useState<EventRecord|null>(events[0]||null);const [d,setD]=useState<R>({});useEffect(()=>{if(selected)setD({...selected})},[selected]);const openDrive=(value?:string)=>{const url=driveUrl(value);if(!url){notify("This Drive folder is not configured.");return;}window.open(url,"_blank","noopener,noreferrer")};const save=async()=>{try{if(!d.title||!d.slug)throw new Error("Title and slug are required.");const body={...d,sort_order:Number(d.sort_order||0),is_active:Boolean(d.is_active),photos_enabled:Boolean(d.photos_enabled),videos_enabled:Boolean(d.videos_enabled),slideshow_enabled:Boolean(d.slideshow_enabled),qr_enabled:Boolean(d.qr_enabled)};if(selected)await supabaseRest("events",{method:"PATCH",token,query:`id=eq.${encodeURIComponent(selected.id)}`,body,prefer:"return=minimal"});else{const created=await supabaseRest<EventRecord[]>("events",{method:"POST",token,body,prefer:"return=representation"});if(created?.[0])setEvents([...events,created[0]])}if(selected)setEvents(events.map((e:EventRecord)=>e.id===selected.id?{...e,...body}:e));notify(selected?"Event saved to draft.":"Event created in draft.")}catch(e){notify(e instanceof Error?e.message:"Event save failed")}};if(!events.length&&!selected)return <Card title={title(child)}><Empty text="No events yet."/></Card>;return <div className="grid gap-5 lg:grid-cols-[300px_1fr]"><Card title="Events"><div className="space-y-2">{events.map((e:EventRecord)=><button key={e.id} type="button" onClick={()=>setSelected(e)} className={`w-full rounded-xl border p-3 text-left ${selected?.id===e.id?"border-primary bg-primary/5":""}`}>{e.title}<div className="text-xs text-muted-foreground">/{e.slug}</div></button>)}</div><Btn onClick={()=>{setSelected(null);setD({title:"",slug:"",description:"",date:"",drive_folder_id:"",photos_drive_folder_id:"",photos_drive_folder_id_2:"",videos_drive_folder_id:"",videos_drive_folder_id_2:"",cover_image_drive_id:"",cover_image:"",venue_name:"",venue_address:"",maps_url:"",sort_order:events.length+1,is_active:true,photos_enabled:true,videos_enabled:true,slideshow_enabled:true,qr_enabled:true})}}><Plus size={15}/>New event</Btn></Card><Card title={selected?`Edit ${selected.title}`:"Create event"}><div className="grid gap-4 md:grid-cols-2"><Input label="Title" value={d.title} onChange={v=>setD({...d,title:v})}/><Input label="Slug" value={d.slug} onChange={v=>setD({...d,slug:v})}/><Input label="Date/time" value={d.date} onChange={v=>setD({...d,date:v})}/><Input label="Sort order" type="number" value={d.sort_order} onChange={v=>setD({...d,sort_order:Number(v)})}/><div className="md:col-span-2"><Area label="Description" value={d.description} onChange={v=>setD({...d,description:v})}/></div><Input label="Drive folder ID" value={d.drive_folder_id} onChange={v=>setD({...d,drive_folder_id:v})}/><Input label="Photos folder ID" value={d.photos_drive_folder_id} onChange={v=>setD({...d,photos_drive_folder_id:v})}/><Input label="Second photos folder ID" value={d.photos_drive_folder_id_2} onChange={v=>setD({...d,photos_drive_folder_id_2:v})}/><Input label="Videos folder ID" value={d.videos_drive_folder_id} onChange={v=>setD({...d,videos_drive_folder_id:v})}/><Input label="Second videos folder ID" value={d.videos_drive_folder_id_2} onChange={v=>setD({...d,videos_drive_folder_id_2:v})}/><div className="md:col-span-2"><div className="rounded-2xl border bg-muted/20 p-4"><div className="mb-3 font-medium">Google Drive folders</div><div className="flex flex-wrap gap-2"><button type="button" disabled={!d.drive_folder_id} onClick={()=>openDrive(d.drive_folder_id)} className="inline-flex min-h-10 items-center justify-center gap-2 rounded-xl border bg-background px-3 text-sm font-medium hover:bg-muted disabled:opacity-50"><ExternalLink size={15}/>Open Drive Folder</button><button type="button" disabled={!d.photos_drive_folder_id} onClick={()=>openDrive(d.photos_drive_folder_id)} className="inline-flex min-h-10 items-center justify-center gap-2 rounded-xl border bg-background px-3 text-sm font-medium hover:bg-muted disabled:opacity-50"><ExternalLink size={15}/>Open Photo Folder</button><button type="button" disabled={!d.videos_drive_folder_id} onClick={()=>openDrive(d.videos_drive_folder_id)} className="inline-flex min-h-10 items-center justify-center gap-2 rounded-xl border bg-background px-3 text-sm font-medium hover:bg-muted disabled:opacity-50"><ExternalLink size={15}/>Open Video Folder</button></div><p className="mt-2 text-xs text-muted-foreground">These buttons only open the configured folder ID/link. They do not expose API keys or credentials.</p></div></div><div className="md:col-span-2"><div className="rounded-2xl border bg-muted/20 p-4"><div className="mb-3 font-medium">Cover image</div><div className="grid gap-4 md:grid-cols-2"><Input label="Cover image Drive ID" value={d.cover_image_drive_id} onChange={v=>setD({...d,cover_image_drive_id:v})} help="Use the Google Drive file ID for the event cover image."/><Input label="Cover image URL" value={d.cover_image} onChange={v=>setD({...d,cover_image:v})} help="Optional direct/public image URL. Drive ID takes precedence when the site resolves the cover."/></div></div></div><Input label="Venue" value={d.venue_name} onChange={v=>setD({...d,venue_name:v})}/><Input label="Address" value={d.venue_address} onChange={v=>setD({...d,venue_address:v})}/><Input label="Maps URL" value={d.maps_url} onChange={v=>setD({...d,maps_url:v})}/>{[["is_active","Active"],["photos_enabled","Photos"],["videos_enabled","Videos"],["slideshow_enabled","Slideshow"],["qr_enabled","QR"]].map(([k,l])=><Toggle key={k} label={l} value={d[k]} onChange={v=>setD({...d,[k]:v})}/>)}</div><div className="mt-5"><Btn primary onClick={()=>void save()}><Check size={15}/>Save Draft</Btn></div></Card></div>}
function Guestbook({token,guestbook,setGuestbook,child,notify}:any){const rows=guestbook.filter((g:any)=>child==="pending"?g.moderation_status==="pending":child==="approved"?(g.approved||g.moderation_status==="approved"):child==="rejected"?g.moderation_status==="rejected":child==="spam"?g.moderation_status==="spam":true);const mod=async(id:string,status:string)=>{try{await setGuestbookModeration(token,id,status);setGuestbook(guestbook.map((g:any)=>g.id===id?{...g,moderation_status:status,approved:status==="approved"}:g));notify(`Message ${status}.`)}catch(e){notify(e instanceof Error?e.message:"Moderation failed")}};return <Card title={`Guestbook · ${title(child)}`}><div className="space-y-3">{rows.map((g:any)=><div key={g.id} className="rounded-xl border p-4"><div className="flex justify-between gap-3"><b>{g.name}</b><span className="text-xs">{g.moderation_status||"pending"}</span></div><p className="my-3 whitespace-pre-wrap text-sm">{g.message}</p><div className="flex flex-wrap gap-2"><Btn onClick={()=>void mod(g.id,"approved")}><Check size={15}/>Approve</Btn><Btn onClick={()=>void mod(g.id,"rejected")}>Reject</Btn><Btn onClick={()=>void mod(g.id,"spam")}>Spam</Btn><Btn onClick={()=>void setGuestbookFeatured(token,g.id,!g.featured)}><Heart size={15}/>Feature</Btn><Btn danger onClick={async()=>{if(confirm("Delete this message?")){await deleteGuestbookMessage(token,g.id);setGuestbook(guestbook.filter((x:any)=>x.id!==g.id));notify("Deleted.")}}}><Trash2 size={15}/></Btn></div></div>)}{!rows.length&&<Empty text="No messages match this panel."/>}</div></Card>}
function Notifications({token,notifications,setNotifications,notify}:any){const [d,setD]=useState({type:"system",title:"",message:"",target:"public"});return <Card title="Notifications"><div className="space-y-4"><Input label="Type" value={d.type} onChange={v=>setD({...d,type:v})}/><Input label="Title" value={d.title} onChange={v=>setD({...d,title:v})}/><Area label="Message" value={d.message} onChange={v=>setD({...d,message:v})}/><Input label="Target" value={d.target} onChange={v=>setD({...d,target:v})}/><Btn primary onClick={async()=>{try{if(!d.title||!d.message)throw new Error("Title and message are required.");const created=await createNotification(token,d);const row=Array.isArray(created)?created[0]:created;setNotifications(row?[row,...notifications]:notifications);notify("Notification created.")}catch(e){notify(e instanceof Error?e.message:"Create failed")}}}><Plus size={15}/>Create</Btn>{notifications.map((n:any)=><div key={n.id} className="rounded-xl border p-3"><b>{n.title}</b><div className="text-xs text-muted-foreground">{n.message}</div><Btn danger onClick={async()=>{await deleteNotification(token,n.id);setNotifications(notifications.filter((x:any)=>x.id!==n.id));notify("Deleted.")}}><Trash2 size={15}/></Btn></div>)}</div></Card>}
function Analytics({analytics}:any){return <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><Metric n="Visitors" v={analytics.total||0}/><Metric n="Gallery views" v={analytics.galleryViews||0}/><Metric n="Reactions" v={analytics.reactionTotal||0}/><Metric n="Event views" v={analytics.eventViews||0}/></div>};function Metric({n,v}:{n:string;v:any}){return <div className="rounded-2xl border bg-card p-5"><div className="text-xs text-muted-foreground">{n}</div><div className="mt-2 text-3xl font-semibold">{v}</div></div>}
function Audit({audit}:any){return <Card title="Audit logs"><div className="space-y-2">{audit.map((x:any)=><div key={x.id} className="rounded-xl border p-3"><b>{x.action}</b><div className="text-xs text-muted-foreground">{x.created_at?new Date(x.created_at).toLocaleString():""} · {x.target_id||""}</div></div>)}{!audit.length&&<Empty text="No audit entries."/>}</div></Card>}
function Security({token,notify}:any){const [codes,setCodes]=useState<any[]>([]);const load=async()=>{try{setCodes(await getEventSecurityCodes(token))}catch(e){notify(e instanceof Error?e.message:"Unable to load security codes")}};useEffect(()=>{void load()},[token]);return <Card title="Event security"><div className="space-y-2">{codes.map(c=><div key={c.id} className="flex items-center gap-3 rounded-xl border p-3"><div className="flex-1"><b>{c.title}</b><div className="text-xs text-muted-foreground">/{c.slug}</div></div><Btn onClick={async()=>{const code=prompt(`New code for ${c.title}`);if(!code)return;try{await changeEventSecurityCode(token,{id:c.id,slug:c.slug},code);notify("Code changed.");void load()}catch(e){notify(e instanceof Error?e.message:"Code change failed")}}}><KeyRound size={15}/>Change</Btn></div>)}</div></Card>}
function Administration({token,notify}:any){const [profiles,setProfiles]=useState<any[]>([]);useEffect(()=>{void (async()=>{try{setProfiles(await supabaseRest<any[]>("profiles",{token,query:"select=id,username,display_name,role,created_at&order=created_at.asc"}))}catch(e){notify(e instanceof Error?e.message:"Administration data unavailable")}})()},[token]);return <Card title="Admin profiles"><div className="space-y-2">{profiles.map(p=><div key={p.id} className="flex items-center gap-3 rounded-xl border p-3"><div className="flex-1"><b>{p.display_name||p.username||"Unnamed"}</b><div className="text-xs text-muted-foreground">{p.username||p.id}</div></div><span className="text-xs">{p.role||"viewer"}</span><Btn onClick={async()=>{const role=prompt("Role",p.role||"viewer");if(!role)return;try{await supabaseRest("rpc/admin_update_profile",{method:"POST",token,body:{p_user_id:p.id,p_role:role,p_username:p.username||"",p_display_name:p.display_name||""}});notify("Profile updated.")}catch(e){notify(e instanceof Error?e.message:"Update failed")}}}><UserCog size={15}/>Edit</Btn></div>)}</div></Card>}
function Backup({events,sections,settings,notify}:any){const exportBackup=()=>{const blob=new Blob([JSON.stringify({schema:1,created_at:new Date().toISOString(),events,sections,settings},null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=`wedding-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();notify("Backup exported.")};return <Card title="Backup & Restore"><p className="text-sm text-muted-foreground">Exports the current draft workspace settings, events and website section structure. Secrets are excluded.</p><div className="mt-4"><Btn primary onClick={exportBackup}><Download size={15}/>Export backup</Btn></div></Card>}
function QR({child,notify}:any){const [v,setV]=useState(window.location.origin);const src=`https://quickchart.io/qr?size=500&text=${encodeURIComponent(v)}`;return <Card title={`QR · ${title(child)}`}><div className="grid gap-5 md:grid-cols-2"><div><Input label="URL or text" value={v} onChange={setV}/><Btn primary onClick={async()=>{const r=await fetch(src);const b=await r.blob();const a=document.createElement("a");a.href=URL.createObjectURL(b);a.download=`wedding-${child}-qr.png`;a.click();notify("QR downloaded.")}}><Download size={15}/>Download PNG</Btn></div><img src={src} alt="QR code" className="mx-auto h-64 w-64 rounded-xl border p-2"/></div></Card>}
function Locations({token,events,notify}:any){const [e,setE]=useState<EventRecord|null>(events[0]||null);const [d,setD]=useState<R>(e||{});useEffect(()=>{if(e)setD(e)},[e]);return <div className="grid gap-5 md:grid-cols-[280px_1fr]"><Card title="Events"><div className="space-y-2">{events.map((x:EventRecord)=><button key={x.id} type="button" onClick={()=>setE(x)} className="w-full rounded-xl border p-3 text-left">{x.title}</button>)}</div></Card><Card title="Location"><Input label="Venue" value={d.venue_name} onChange={v=>setD({...d,venue_name:v})}/><Area label="Address" value={d.venue_address} onChange={v=>setD({...d,venue_address:v})}/><Input label="Google Maps URL" value={d.maps_url} onChange={v=>setD({...d,maps_url:v})}/><Btn primary onClick={async()=>{if(!e)return;try{await supabaseRest("events",{method:"PATCH",token,query:`id=eq.${encodeURIComponent(e.id)}`,body:{venue_name:d.venue_name,venue_address:d.venue_address,maps_url:d.maps_url}});notify("Location saved to draft.")}catch(err){notify(err instanceof Error?err.message:"Save failed")}}}><Check size={15}/>Save Draft</Btn></Card></div>}
function Publishing({token,notify}:any){const [r,setR]=useState<any[]>([]);const load=async()=>{try{const response=await fetch("/api/admin-advanced?action=versions",{headers:{Authorization:`Bearer ${token}`}});const data=await response.json().catch(()=>({}));if(!response.ok||!data.ok)throw new Error(data.error||"Revision load failed");setR(data.versions||[])}catch(e){notify(e instanceof Error?e.message:"Revision load failed")}};useEffect(()=>{void load()},[token]);const rollback=async(id:string)=>{if(!confirm("Restore this version into the draft workspace? It will not go live until you publish it."))return;try{const response=await fetch("/api/admin-advanced?action=rollback",{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({id})});const data=await response.json().catch(()=>({}));if(!response.ok||!data.ok)throw new Error(data.error||"Rollback failed");notify(data.message||"Rollback prepared as draft.");await load()}catch(e){notify(e instanceof Error?e.message:"Rollback failed")}};return <Card title="Publishing & Version History" action={<Btn onClick={()=>void load()}><RefreshCw size={15}/>Refresh</Btn>}><div className="mb-4 rounded-2xl border border-dashed p-4 text-sm text-muted-foreground">Save changes to the draft workspace, use Preview to inspect them, then Publish. Rollback creates a new draft from the selected published version; it never changes the live site immediately.</div><div className="space-y-2">{r.map(x=><div key={x.id} className="flex flex-wrap items-center gap-3 rounded-xl border p-3"><div className="min-w-0 flex-1"><b>Version {x.version_no}</b><div className="text-xs text-muted-foreground">{x.status} · {x.label||"Revision"} · {x.created_at?new Date(x.created_at).toLocaleString():""}</div></div>{x.status==="published"&&<Btn onClick={()=>void rollback(x.id)}><RefreshCw size={15}/>Restore to Draft</Btn>}</div>)}{!r.length&&<Empty text="No revisions found."/>}</div></Card>}
function Diagnostics({health,notify}:any){const [r,setR]=useState<any[]>([]);return <Card title="Testing & Diagnostics" action={<Btn primary onClick={()=>{setR(health.map((h:Health)=>({name:h.label,ok:h.status!=="error",detail:h.detail})));notify("Diagnostics completed.")}}><Activity size={15}/>Run checks</Btn>}><div className="space-y-2">{r.map(x=><div key={x.name} className="rounded-xl border p-3">{x.ok?"✓":"✕"} <b>{x.name}</b><div className="text-xs text-muted-foreground">{x.detail}</div></div>)}</div></Card>}
function System({health,child}:any){return <Card title={`System · ${title(child)}`}><div className="space-y-2">{health.map((h:Health)=><div key={h.label} className="rounded-xl border p-3"><b>{h.label}</b><div className="text-xs text-muted-foreground">{h.detail}</div></div>)}</div></Card>}
function Generic({module,child}:any){return <Card title={title(child)}><p className="text-sm text-muted-foreground">{module.description}</p><div className="mt-4 rounded-2xl border border-dashed p-5 text-sm text-muted-foreground">This panel uses the existing generic workflow. Dedicated Website, Events, Guestbook, Security, Publishing and Diagnostics controls are connected.</div></Card>}
