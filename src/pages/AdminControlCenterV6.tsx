import { useEffect, useMemo, useState, type ReactNode } from "react";
import { Activity, ArrowDown, ArrowUp, BarChart3, Bell, Check, ChevronRight, Download, Eye, EyeOff, ExternalLink, FileArchive, Heart, KeyRound, LogOut, Maximize2, Menu, Minimize2, Moon, Pencil, Plus, QrCode, RefreshCw, Rocket, Search, ShieldCheck, Sparkles, Sun, Trash2, UserCog, X } from "lucide-react";
import { ADMIN_MODULES, GROUP_LABELS, type AdminModule } from "@/admin/moduleRegistry";
import { isSupabaseConfigured, supabaseRest } from "@/lib/supabase";
import { getAdminEvents, getAdminFeaturedGuestbook, getNotifications, getAuditLogs, getAdminSettings, getAdminAnalytics, getAdminHomepageSections, updateHomepageSections, saveSiteSetting, writeAdminAudit, setGuestbookModeration, setGuestbookFeatured, deleteGuestbookMessage, createNotification, deleteNotification, getEventSecurityCodes, changeEventSecurityCode, type EventRecord } from "@/lib/supabaseData";
import WebsiteContentPanel from "./WebsiteContentPanel";

type R=Record<string,any>; type Health={label:string;status:"ok"|"warn"|"error";detail:string};
const TOKEN_KEY="wedding-admin-access-token", REFRESH_KEY="wedding-admin-refresh-token", PREVIEW_KEY="wedding-admin-preview-snapshot-v1";
const SECTION_TO_CHILD:Record<string,string>={hero:"home",couple:"couple",story:"story",gallery:"gallery",events:"events",rsvp:"rsvp",guestbook:"guestbook",footer:"footer"};
const title=(x:string)=>x.replaceAll("-"," ").replace(/\b\w/g,c=>c.toUpperCase());
const driveUrl=(v?:string)=>{const x=String(v||"").trim();return !x?"":/^https?:\/\//i.test(x)?x:`https://drive.google.com/drive/folders/${encodeURIComponent(x)}`};
function Btn({children,onClick,primary=false,danger=false,disabled=false}:{children:ReactNode;onClick?:()=>void;primary?:boolean;danger?:boolean;disabled?:boolean}){return <button type="button" disabled={disabled} onClick={onClick} className={`inline-flex min-h-10 items-center justify-center gap-2 rounded-xl border px-3 text-sm font-medium hover:bg-muted disabled:opacity-50 ${primary?"border-primary bg-primary text-primary-foreground":danger?"border-red-200 text-red-700":"bg-background"}`}>{children}</button>}
function Input({label,value,onChange,type="text",placeholder="",help}:{label:string;value:any;onChange:(v:string)=>void;type?:string;placeholder?:string;help?:string}){return <label className="block min-w-0 text-sm font-medium"><span>{label}</span><input type={type} value={value??""} placeholder={placeholder} onChange={e=>onChange(e.target.value)} className="mt-1 h-11 w-full max-w-full rounded-xl border bg-background px-3 outline-none focus:ring-2 focus:ring-primary/20"/>{help&&<span className="mt-1 block text-xs text-muted-foreground">{help}</span>}</label>}
function Area({label,value,onChange}:{label:string;value:any;onChange:(v:string)=>void}){return <label className="block min-w-0 text-sm font-medium"><span>{label}</span><textarea value={value??""} onChange={e=>onChange(e.target.value)} className="mt-1 min-h-28 w-full rounded-xl border bg-background p-3"/></label>}
function Toggle({label,value,onChange}:{label:string;value:any;onChange:(v:boolean)=>void}){return <label className="flex min-h-11 items-center justify-between gap-4 rounded-xl border p-3 text-sm"><span className="min-w-0">{label}</span><input type="checkbox" checked={Boolean(value)} onChange={e=>onChange(e.target.checked)} className="h-5 w-5 shrink-0"/></label>}
function Card({title:heading,children,action}:{title:string;children:ReactNode;action?:ReactNode}){return <section className="min-w-0 overflow-hidden rounded-3xl border bg-card p-5 shadow-sm md:p-6"><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><h2 className="font-semibold">{heading}</h2>{action}</div>{children}</section>}
function Empty({text}:{text:string}){return <div className="rounded-2xl border border-dashed p-8 text-center text-sm text-muted-foreground">{text}</div>}

export default function AdminControlCenterV6(){
 const [token,setToken]=useState(()=>sessionStorage.getItem(TOKEN_KEY)||""); const [active,setActive]=useState("dashboard"); const [child,setChild]=useState(""); const [search,setSearch]=useState(""); const [mobile,setMobile]=useState(false); const [dark,setDark]=useState(()=>localStorage.getItem("admin-theme")==="dark"); const [busy,setBusy]=useState(false); const [toast,setToast]=useState(""); const [fullscreen,setFullscreen]=useState(false);
 const [events,setEvents]=useState<EventRecord[]>([]),[guestbook,setGuestbook]=useState<R[]>([]),[notifications,setNotifications]=useState<R[]>([]),[audit,setAudit]=useState<R[]>([]),[settings,setSettings]=useState<R>({}),[analytics,setAnalytics]=useState<R>({}),[sections,setSections]=useState<R[]>([]),[health,setHealth]=useState<Health[]>([]);
 const modules=useMemo(()=>{const q=search.trim().toLowerCase();return q?ADMIN_MODULES.filter(m=>`${m.label} ${m.description} ${m.children.map(c=>c.label).join(" ")}`.toLowerCase().includes(q)):ADMIN_MODULES},[search]);
 const current=ADMIN_MODULES.find(m=>m.id===active)||ADMIN_MODULES[0];
 const websiteChildren=useMemo(()=>{if(active!=="website")return current.children;const enabled=sections.filter(s=>Boolean(s.enabled)&&SECTION_TO_CHILD[s.key]).sort((a,b)=>Number(a.sort_order||0)-Number(b.sort_order||0)).map(s=>({id:SECTION_TO_CHILD[s.key],label:s.label||title(SECTION_TO_CHILD[s.key]),description:`Edit the enabled ${String(s.label||s.key).toLowerCase()} section.`}));return [{id:"section-manager",label:"section manager",description:"Master website section visibility and order."},...enabled,{id:"navigation",label:"navigation",description:"Header and navigation links."}].filter((x,i,a)=>a.findIndex(y=>y.id===x.id)===i)},[active,current.children,sections]);
 useEffect(()=>{document.documentElement.classList.toggle("dark",dark);localStorage.setItem("admin-theme",dark?"dark":"light")},[dark]);
 useEffect(()=>{const f=()=>setFullscreen(Boolean(document.fullscreenElement));document.addEventListener("fullscreenchange",f);f();return()=>document.removeEventListener("fullscreenchange",f)},[]);
 useEffect(()=>{const items=active==="website"?websiteChildren:current.children;if(!items.some(c=>c.id===child))setChild(items[0]?.id||"")},[active,current.children,websiteChildren,child]);
 const notify=(x:string)=>{setToast(x);window.setTimeout(()=>setToast(""),3500)};
 const load=async()=>{if(!token)return;setBusy(true);const h:Health[]=[{label:"Supabase",status:isSupabaseConfigured?"ok":"error",detail:isSupabaseConfigured?"Configured":"Missing configuration"}];try{const ok=await supabaseRest<boolean>("rpc/is_admin",{method:"POST",token,body:{}});h.push({label:"Admin authorization",status:ok?"ok":"error",detail:ok?"Verified":"Denied"});if(!ok){sessionStorage.clear();setToken("");setBusy(false);return}}catch(e){h.push({label:"Admin authorization",status:"error",detail:e instanceof Error?e.message:"Failed"})}try{const [ev,gb,no,al,st,an,hs]=await Promise.all([getAdminEvents(token),getAdminFeaturedGuestbook(token),getNotifications(token),getAuditLogs(token),getAdminSettings(token),getAdminAnalytics(token),getAdminHomepageSections(token)]);setEvents(ev||[]);setGuestbook(gb||[]);setNotifications(no||[]);setAudit(al||[]);setSettings(Object.fromEntries((st||[]).map(x=>[x.key,x.value])));setAnalytics(an||{});setSections(hs||[]);h.push({label:"Admin data",status:"ok",detail:"Loaded"})}catch(e){h.push({label:"Admin data",status:"warn",detail:e instanceof Error?e.message:"Some data unavailable"})}try{const r=await fetch("/api/admin-users?action=list_sessions",{headers:{Authorization:`Bearer ${token}`}});h.push({label:"Session service",status:r.ok?"ok":"warn",detail:r.ok?"Reachable":"Unavailable"})}catch{h.push({label:"Session service",status:"warn",detail:"Unavailable"})}setHealth(h);setBusy(false)};
 useEffect(()=>{void load()},[token]);
 const go=(id:string)=>{setActive(id);setMobile(false);window.scrollTo({top:0,behavior:"smooth"})}; const logout=()=>{sessionStorage.removeItem(TOKEN_KEY);sessionStorage.removeItem(REFRESH_KEY);setToken("")};
 const fullscreenToggle=async()=>{try{if(document.fullscreenElement)await document.exitFullscreen();else await document.documentElement.requestFullscreen()}catch(e){notify(e instanceof Error?e.message:"Fullscreen is unavailable")}};
 const preview=async()=>{try{const r=await fetch("/api/admin-advanced?action=draft",{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({label:`Preview ${new Date().toLocaleString()}`})});const d=await r.json().catch(()=>({}));if(!r.ok||!d.ok)throw new Error(d.error||"Unable to create preview");localStorage.setItem(PREVIEW_KEY,JSON.stringify({revision_id:d.revision.id,snapshot:d.revision.snapshot,created_at:new Date().toISOString()}));window.open("/?preview=draft","_blank","noopener,noreferrer");notify("Draft preview opened.")}catch(e){notify(e instanceof Error?e.message:"Preview failed")}};
 const publish=async()=>{try{setBusy(true);const r=await fetch("/api/admin-advanced?action=publish",{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({label:`Publish ${new Date().toLocaleString()}`})});const d=await r.json().catch(()=>({}));if(!r.ok||!d.ok)throw new Error(d.error||"Publish failed");await writeAdminAudit(token,"publish_homepage",String(d.revision_id),{version_no:d.version_no});notify(`Published Version ${d.version_no}.`);await load()}catch(e){notify(e instanceof Error?e.message:"Publish failed")}finally{setBusy(false)}};
 if(!token)return <div className="grid min-h-screen place-items-center p-6"><div className="rounded-3xl border bg-card p-8 text-center"><Sparkles className="mx-auto mb-3"/><h1 className="text-2xl font-semibold">Admin session required</h1></div></div>;
 return <div className="min-h-screen min-w-0 bg-muted/20"><header className="sticky top-0 z-50 border-b bg-background/95 backdrop-blur"><div className="flex h-16 min-w-0 items-center gap-2 px-3 md:px-5"><Btn onClick={()=>setMobile(true)}><Menu size={18}/></Btn><button type="button" onClick={()=>go("dashboard")} className="flex min-w-0 items-center gap-2 font-semibold"><span className="grid h-9 w-9 shrink-0 place-items-center rounded-xl bg-primary text-primary-foreground"><Sparkles size={17}/></span><span className="hidden truncate sm:block">Wedding Admin</span></button><div className="relative ml-auto hidden max-w-md flex-1 md:block"><Search className="absolute left-3 top-1/2 -translate-y-1/2" size={16}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search modules or selections…" className="h-10 w-full rounded-xl border pl-9"/></div><Btn onClick={()=>void fullscreenToggle()}>{fullscreen?<Minimize2 size={17}/>:<Maximize2 size={17}/>}</Btn><Btn onClick={()=>void preview()} disabled={busy}><Eye size={16}/><span className="hidden sm:inline">Preview</span></Btn><Btn primary onClick={()=>void publish()} disabled={busy}><Rocket size={16}/><span className="hidden sm:inline">{busy?"Publishing…":"Publish"}</span></Btn><Btn onClick={()=>setDark(v=>!v)}>{dark?<Sun size={17}/>:<Moon size={17}/>}</Btn><Btn onClick={logout}><LogOut size={16}/></Btn></div></header><div className="flex min-w-0"><aside className="sticky top-16 hidden h-[calc(100vh-4rem)] w-64 shrink-0 overflow-y-auto border-r bg-background p-3 md:block">{(["wedding","insights","management","system"] as const).map(g=><div key={g} className="mb-5"><div className="px-2 py-2 text-[10px] font-semibold tracking-widest text-muted-foreground">{GROUP_LABELS[g]}</div>{modules.filter(m=>m.group===g).map(m=><button key={m.id} type="button" onClick={()=>go(m.id)} className={`mb-1 flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm ${active===m.id?"bg-primary text-primary-foreground":"hover:bg-muted"}`}><m.icon size={17}/><span className="min-w-0 truncate">{m.number}. {m.label}</span></button>)}</div>)}</aside>{mobile&&<div className="fixed inset-0 z-[70] md:hidden"><button aria-label="Close navigation" className="absolute inset-0 bg-black/40" onClick={()=>setMobile(false)}/><aside className="relative h-full w-[88vw] max-w-sm overflow-y-auto bg-background p-4"><div className="mb-4 flex justify-between"><b>Administration · 24 Panels</b><Btn onClick={()=>setMobile(false)}><X size={17}/></Btn></div>{modules.map(m=><button key={m.id} type="button" onClick={()=>go(m.id)} className={`mb-1 flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left hover:bg-muted ${active===m.id?"bg-primary text-primary-foreground":""}`}><m.icon size={17}/><span className="min-w-0 truncate">{m.number}. {m.label}</span></button>)}</aside></div>}<main className="min-w-0 flex-1 p-3 sm:p-4 md:p-7"><div className="mx-auto min-w-0 max-w-[1550px]"><div className="mb-5 flex min-w-0 flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div className="min-w-0"><div className="truncate text-xs text-muted-foreground">Admin <ChevronRight size={12} className="inline"/> {current.number}. {current.label}</div><h1 className="truncate text-2xl font-semibold sm:text-3xl">{current.label}</h1><p className="mt-1 max-w-3xl text-sm text-muted-foreground">{current.description}</p></div><Btn onClick={()=>void load()} disabled={busy}><RefreshCw className={busy?"animate-spin":""} size={15}/><span className="hidden sm:inline">Refresh</span></Btn></div>{active==="dashboard"?<Dashboard {...{events,guestbook,analytics,health,go}}/>:<ModuleView {...{module:current,child,setChild,token,settings,setSettings,events,setEvents,guestbook,setGuestbook,notifications,setNotifications,analytics,audit,health,sections,setSections,notify,websiteChildren}}/>}</div></main></div>{toast&&<div className="fixed inset-x-3 top-20 z-[100] mx-auto max-w-xl rounded-2xl border bg-background p-4 text-sm shadow-xl sm:inset-x-auto sm:bottom-5 sm:right-5 sm:top-auto" role="status" aria-live="polite">{toast}</div>}</div>
}
function Dashboard({events,guestbook,analytics,health,go}:any){return <div className="space-y-6"><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5">{[["Events",events.length,"events"],["Guestbook",guestbook.length,"guestbook"],["Visitors",analytics.total||0,"analytics"],["Reactions",analytics.reactionTotal||0,"interactions"],["Health",`${health.filter((h:Health)=>h.status==="ok").length}/${health.length}`,"system"]].map(([a,b,id])=><button key={String(a)} type="button" onClick={()=>go(String(id))} className="min-w-0 rounded-2xl border bg-card p-4 text-left"><div className="text-xs text-muted-foreground">{a}</div><div className="mt-2 text-3xl font-semibold">{b}</div></button>)}</div><Card title="All 24 panels"><div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{ADMIN_MODULES.map(m=><Btn key={m.id} onClick={()=>go(m.id)}>{m.number}. {m.label}<ChevronRight size={15}/></Btn>)}</div></Card><Card title="System health"><div className="grid gap-3 md:grid-cols-2">{health.map(h=><div key={h.label} className="rounded-xl border p-3"><b>{h.label}</b><div className="text-xs text-muted-foreground">{h.detail}</div></div>)}</div></Card></div>}
function SelectionTabs({module,items,selected,onSelect}:{module:AdminModule;items:{id:string;label:string;description?:string}[];selected:string;onSelect:(id:string)=>void}){return <div className="rounded-2xl border bg-background p-2"><div className="mb-2 px-2 text-xs text-muted-foreground">{module.label} selections</div><div className="grid grid-cols-2 gap-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5">{items.map(c=><button key={c.id} type="button" onClick={()=>onSelect(c.id)} className={`min-h-11 min-w-0 rounded-xl border px-3 py-2 text-left text-sm font-medium ${selected===c.id?"border-primary bg-primary text-primary-foreground":"bg-background hover:bg-muted"}`}><span className="block truncate">{title(c.id)}</span><span className={`mt-0.5 block truncate text-[11px] ${selected===c.id?"text-primary-foreground/80":"text-muted-foreground"}`}>{c.description||`Manage ${title(c.id).toLowerCase()}.`}</span></button>)}</div></div>}
function ModuleView(p:any){const {module,child,setChild}=p;const tabs=module.id==="website"?p.websiteChildren:module.children;const selected=tabs.some((x:any)=>x.id===child)?child:tabs[0]?.id||"";const website=module.id==="website";return <div className="space-y-4"><SelectionTabs module={module} items={tabs} selected={selected} onSelect={setChild}/>{website&&selected==="section-manager"?<Homepage {...p}/>:website&&["home","couple","story","gallery","rsvp","footer","navigation"].includes(selected)?<WebsiteContentPanel {...p}/>:module.id==="events"?<Events {...p}/>:module.id==="guestbook"?<Guestbook {...p}/>:module.id==="notifications"?<Notifications {...p}/>:module.id==="analytics"?<Analytics {...p}/>:module.id==="audit"?<Audit {...p}/>:module.id==="security"?<Security {...p}/>:module.id==="administration"?<Administration {...p}/>:module.id==="backup"?<Backup {...p}/>:module.id==="qr"?<QR {...p}/>:module.id==="locations"?<Locations {...p}/>:module.id==="publishing"?<Publishing {...p}/>:module.id==="diagnostics"?<Diagnostics {...p}/>:module.id==="system"?<System {...p}/>:<SelectionWorkspace {...p} selected={selected}/>}</div>}
function Homepage({token,sections,setSections,setChild,notify}:any){const [rows,setRows]=useState<R[]>(sections);useEffect(()=>setRows(sections),[sections]);const move=(i:number,d:number)=>{const n=[...rows],j=i+d;if(j<0||j>=n.length)return;[n[i],n[j]]=[n[j],n[i]];setRows(n)};const save=async()=>{try{const normalized=rows.map((r,i)=>({...r,sort_order:i+1,enabled:Boolean(r.enabled)}));await updateHomepageSections(token,normalized);setSections(normalized);notify("Section Manager saved to Draft. Live website unchanged.")}catch(e){notify(e instanceof Error?e.message:"Section save failed")}};return <Card title="Section Manager · master website order" action={<Btn primary onClick={()=>void save()}><Check size={15}/>Save Draft</Btn>}><div className="mb-4 rounded-2xl border border-dashed p-4 text-sm text-muted-foreground">Only enabled sections appear as Website selections, in this exact order. Disable a section to remove its editor from Website.</div><div className="space-y-3">{rows.map((s,i)=><div key={s.key} className="rounded-2xl border p-4"><div className="font-semibold break-words">{s.label}</div><div className="mt-1 text-xs text-muted-foreground">{s.key}</div><div className="mt-3 flex flex-wrap gap-2"><Btn onClick={()=>setRows(x=>x.map((a,k)=>k===i?{...a,enabled:!a.enabled}:a))}>{s.enabled?<Eye size={16}/>:<EyeOff size={16}/>} {s.enabled?"Enabled":"Disabled"}</Btn><Btn disabled={!SECTION_TO_CHILD[s.key]} onClick={()=>{const id=SECTION_TO_CHILD[s.key];if(id){setChild(id);window.scrollTo({top:0,behavior:"smooth"})}}}><Pencil size={16}/>Edit</Btn><Btn onClick={()=>window.open(`/?section=${encodeURIComponent(s.key)}`,"_blank","noopener,noreferrer")}><ExternalLink size={16}/>View</Btn><Btn onClick={()=>move(i,-1)} disabled={!i}><ArrowUp size={16}/>Up</Btn><Btn onClick={()=>move(i,1)} disabled={i===rows.length-1}><ArrowDown size={16}/>Down</Btn></div></div>)}</div></Card>}
function Events({token,events,setEvents,child,notify}:any){const [selected,setSelected]=useState<EventRecord|null>(events[0]||null);const [d,setD]=useState<R>({});useEffect(()=>{if(selected)setD({...selected});else setD({title:"",slug:"",description:"",date:"",drive_folder_id:"",photos_drive_folder_id:"",photos_drive_folder_id_2:"",videos_drive_folder_id:"",videos_drive_folder_id_2:"",cover_image_drive_id:"",cover_image:"",venue_name:"",venue_address:"",maps_url:"",sort_order:events.length+1,is_active:true,photos_enabled:true,videos_enabled:true,slideshow_enabled:true,qr_enabled:true})},[selected,events.length]);const save=async()=>{try{if(!d.title||!d.slug)throw new Error("Title and slug are required.");const body={...d,sort_order:Number(d.sort_order||0),is_active:Boolean(d.is_active),photos_enabled:Boolean(d.photos_enabled),videos_enabled:Boolean(d.videos_enabled),slideshow_enabled:Boolean(d.slideshow_enabled),qr_enabled:Boolean(d.qr_enabled)};if(selected){await supabaseRest("events",{method:"PATCH",token,query:`id=eq.${encodeURIComponent(selected.id)}`,body,prefer:"return=minimal"});setEvents(events.map(e=>e.id===selected.id?{...e,...body}:e))}else{const created=await supabaseRest<EventRecord[]>("events",{method:"POST",token,body,prefer:"return=representation"});if(created?.[0]){setEvents([...events,created[0]]);setSelected(created[0])}}notify("Event saved to Draft. Live website unchanged.")}catch(e){notify(e instanceof Error?e.message:"Event save failed")}};const open=(v?:string)=>{const u=driveUrl(v);if(!u){notify("Drive folder is not configured.");return}window.open(u,"_blank","noopener,noreferrer")};return <div className="grid gap-5 lg:grid-cols-[300px_minmax(0,1fr)]"><Card title="Events"><div className="max-h-[65vh] space-y-2 overflow-y-auto">{events.map(e=><button key={e.id} type="button" onClick={()=>setSelected(e)} className={`w-full rounded-xl border p-3 text-left ${selected?.id===e.id?"border-primary bg-primary/5":""}`}>{e.title}<div className="truncate text-xs text-muted-foreground">/{e.slug}</div></button>)}</div><div className="mt-3"><Btn onClick={()=>setSelected(null)}><Plus size={15}/>New event</Btn></div></Card><Card title={selected?`Edit ${selected.title} · ${title(child)}`:`Create event · ${title(child)}`}><div className="grid gap-4 md:grid-cols-2"><Input label="Title" value={d.title} onChange={v=>setD({...d,title:v})}/><Input label="Slug" value={d.slug} onChange={v=>setD({...d,slug:v})}/><Input label="Date/time" value={d.date} onChange={v=>setD({...d,date:v})}/><Input label="Sort order" type="number" value={d.sort_order} onChange={v=>setD({...d,sort_order:Number(v)})}/><div className="md:col-span-2"><Area label="Description" value={d.description} onChange={v=>setD({...d,description:v})}/></div><Input label="Drive folder ID" value={d.drive_folder_id} onChange={v=>setD({...d,drive_folder_id:v})}/><Input label="Photos folder ID" value={d.photos_drive_folder_id} onChange={v=>setD({...d,photos_drive_folder_id:v})}/><Input label="Videos folder ID" value={d.videos_drive_folder_id} onChange={v=>setD({...d,videos_drive_folder_id:v})}/><Input label="Venue" value={d.venue_name} onChange={v=>setD({...d,venue_name:v})}/><Input label="Address" value={d.venue_address} onChange={v=>setD({...d,venue_address:v})}/><Input label="Maps URL" value={d.maps_url} onChange={v=>setD({...d,maps_url:v})}/>{[["is_active","Active"],["photos_enabled","Photos"],["videos_enabled","Videos"],["slideshow_enabled","Slideshow"],["qr_enabled","QR"]].map(([k,l])=><Toggle key={k} label={l} value={d[k]} onChange={v=>setD({...d,[k]:v})}/>)}</div><div className="mt-4 flex flex-wrap gap-2"><Btn disabled={!d.drive_folder_id} onClick={()=>open(d.drive_folder_id)}><ExternalLink size={15}/>Drive Folder</Btn><Btn disabled={!d.photos_drive_folder_id} onClick={()=>open(d.photos_drive_folder_id)}><ExternalLink size={15}/>Photo Folder</Btn><Btn disabled={!d.videos_drive_folder_id} onClick={()=>open(d.videos_drive_folder_id)}><ExternalLink size={15}/>Video Folder</Btn><Btn primary onClick={()=>void save()}><Check size={15}/>Save Draft</Btn></div></Card></div>}
function Guestbook({token,guestbook,setGuestbook,child,notify}:any){const rows=guestbook.filter(g=>child==="pending"?g.moderation_status==="pending":child==="approved"?(g.approved||g.moderation_status==="approved"):child==="rejected"?g.moderation_status==="rejected":child==="spam"?g.moderation_status==="spam":true);const mod=async(id:string,status:string)=>{try{await setGuestbookModeration(token,id,status);setGuestbook(guestbook.map(g=>g.id===id?{...g,moderation_status:status,approved:status==="approved"}:g));notify(`Message ${status}.`)}catch(e){notify(e instanceof Error?e.message:"Moderation failed")}};return <Card title={`Guestbook · ${title(child)}`}><div className="space-y-3">{rows.map(g=><div key={g.id} className="rounded-xl border p-4"><div className="flex justify-between gap-3"><b>{g.name}</b><span className="text-xs">{g.moderation_status||"pending"}</span></div><p className="my-3 whitespace-pre-wrap text-sm">{g.message}</p><div className="flex flex-wrap gap-2"><Btn onClick={()=>void mod(g.id,"approved")}><Check size={15}/>Approve</Btn><Btn onClick={()=>void mod(g.id,"rejected")}>Reject</Btn><Btn onClick={()=>void mod(g.id,"spam")}>Spam</Btn><Btn onClick={()=>void setGuestbookFeatured(token,g.id,!g.featured)}><Heart size={15}/>Feature</Btn><Btn danger onClick={async()=>{if(confirm("Delete this message?")){await deleteGuestbookMessage(token,g.id);setGuestbook(guestbook.filter(x=>x.id!==g.id));notify("Message deleted.")}}}><Trash2 size={15}/></Btn></div></div>)}{!rows.length&&<Empty text="No messages match this selection."/>}</div></Card>}
function Notifications({token,notifications,setNotifications,notify,child}:any){const [d,setD]=useState({type:child||"system",title:"",message:"",target:"public"});useEffect(()=>setD(x=>({...x,type:child||"system"})),[child]);const create=async()=>{try{if(!d.title||!d.message)throw new Error("Title and message are required.");const created=await createNotification(token,d);const row=Array.isArray(created)?created[0]:created;if(row)setNotifications([row,...notifications]);notify("Notification created.")}catch(e){notify(e instanceof Error?e.message:"Create failed")}};return <Card title={`Notifications · ${title(child)}`}><div className="grid gap-4"><Input label="Type" value={d.type} onChange={v=>setD({...d,type:v})}/><Input label="Title" value={d.title} onChange={v=>setD({...d,title:v})}/><Area label="Message" value={d.message} onChange={v=>setD({...d,message:v})}/><Input label="Target" value={d.target} onChange={v=>setD({...d,target:v})}/><Btn primary onClick={()=>void create()}><Plus size={15}/>Create Notification</Btn>{notifications.filter(n=>child==="center"||n.type===child).map(n=><div key={n.id} className="flex flex-wrap items-center gap-3 rounded-xl border p-3"><div className="min-w-0 flex-1"><b>{n.title}</b><div className="break-words text-xs text-muted-foreground">{n.message}</div></div><Btn danger onClick={async()=>{try{await deleteNotification(token,n.id);setNotifications(notifications.filter(x=>x.id!==n.id));notify("Notification deleted.")}catch(e){notify(e instanceof Error?e.message:"Delete failed")}}}><Trash2 size={15}/></Btn></div>)}</div></Card>}
function Analytics({analytics,child}:any){const map:any={overview:["Visitors",analytics.total||0],visitors:["Visitors",analytics.total||0],"page-views":["Page views",analytics.total||0],"event-views":["Event views",analytics.byType?.event_view||0],"gallery-views":["Gallery views",analytics.byType?.photo_open||0],"video-views":["Video views",analytics.byType?.video_open||0],favorites:["Favorites",analytics.favoriteTotal||0],"qr-scans":["QR scans",analytics.qrScans||0]};const [n,v]=map[child]||map.overview;return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric n={n} v={v}/><Metric n="Tracked views" v={analytics.total||0}/><Metric n="Reactions" v={analytics.reactionTotal||0}/><Metric n="Current selection" v={title(child)}/></div>};function Metric({n,v}:{n:string;v:any}){return <div className="rounded-2xl border bg-card p-5"><div className="text-xs text-muted-foreground">{n}</div><div className="mt-2 truncate text-3xl font-semibold">{v}</div></div>}
function Audit({audit,child}:any){const rows=child==="all-activity"?audit:audit.filter(x=>{const a=String(x.action||"").toLowerCase();return child.includes("security")?a.includes("security")||a.includes("code"):child.includes("permission")?a.includes("permission")||a.includes("profile"):child.includes("media")?a.includes("media"):child.includes("content")?a.includes("content")||a.includes("homepage")||a.includes("event"):child.includes("admin")?a.includes("admin")||a.includes("profile"):child.includes("login")?a.includes("login")||a.includes("session"):true});return <Card title={`Audit Logs · ${title(child)}`}><div className="space-y-2">{rows.map(x=><div key={x.id} className="rounded-xl border p-3"><b>{x.action}</b><div className="break-words text-xs text-muted-foreground">{x.created_at?new Date(x.created_at).toLocaleString():""} · {x.target_id||""}</div></div>)}{!rows.length&&<Empty text="No audit entries for this selection."/>}</div></Card>}
function Security({token,notify,child}:any){const [codes,setCodes]=useState<any[]>([]);const load=async()=>{try{setCodes(await getEventSecurityCodes(token))}catch(e){notify(e instanceof Error?e.message:"Unable to load security data")}};useEffect(()=>{void load()},[token]);return <Card title={`Security · ${title(child)}`}><div className="mb-4 rounded-2xl border border-dashed p-4 text-sm text-muted-foreground">This selection is scoped to administrator authentication, sessions, rate limits, API protection, database/storage policy, or tracked security events.</div><div className="space-y-2">{codes.map(c=><div key={c.id} className="flex flex-wrap items-center gap-3 rounded-xl border p-3"><div className="min-w-0 flex-1"><b>{c.title}</b><div className="truncate text-xs text-muted-foreground">/{c.slug}</div></div><Btn onClick={async()=>{const code=prompt(`New code for ${c.title}`);if(!code)return;try{await changeEventSecurityCode(token,{id:c.id,slug:c.slug},code);notify("Security code changed.");void load()}catch(e){notify(e instanceof Error?e.message:"Code change failed")}}}><KeyRound size={15}/>Change</Btn></div>)}{!codes.length&&<Empty text="No event security records found."/>}</div></Card>}
function Administration({token,notify,child}:any){const [profiles,setProfiles]=useState<any[]>([]);useEffect(()=>{void(async()=>{try{setProfiles(await supabaseRest<any[]>("profiles",{token,query:"select=id,username,display_name,role,created_at&order=created_at.asc"}))}catch(e){notify(e instanceof Error?e.message:"Administration data unavailable")}})()},[token]);return <Card title={`Administration · ${title(child)}`}><div className="mb-4 rounded-2xl border border-dashed p-4 text-sm text-muted-foreground">{child==="my-profile"?"Current profile and identity.":child==="admin-users"?"Administrator account list.":child==="roles"?"Role assignments.":child==="permissions"?"Module permission matrix.":"Access management."}</div><div className="space-y-2">{profiles.map(p=><div key={p.id} className="flex flex-wrap items-center gap-3 rounded-xl border p-3"><div className="min-w-0 flex-1"><b className="break-words">{p.display_name||p.username||"Unnamed"}</b><div className="truncate text-xs text-muted-foreground">{p.username||p.id}</div></div><span className="text-xs">{p.role||"viewer"}</span><Btn onClick={async()=>{const role=prompt("Role",p.role||"viewer");if(!role)return;try{await supabaseRest("rpc/admin_update_profile",{method:"POST",token,body:{p_user_id:p.id,p_role:role,p_username:p.username||"",p_display_name:p.display_name||""}});notify("Profile updated.")}catch(e){notify(e instanceof Error?e.message:"Update failed")}}}><UserCog size={15}/>Edit</Btn></div>)}{!profiles.length&&<Empty text="No administrator profiles returned."/>}</div></Card>}
function Backup({events,sections,settings,notify,child}:any){const exportBackup=()=>{const blob=new Blob([JSON.stringify({schema:2,created_at:new Date().toISOString(),events,sections,settings},null,2)],{type:"application/json"});const u=URL.createObjectURL(blob),a=document.createElement("a");a.href=u;a.download=`wedding-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(u);notify("Backup exported.")};return <Card title={`Backup & Restore · ${title(child)}`}><p className="text-sm text-muted-foreground">Create/export a recoverable snapshot of the currently loaded Draft-related data.</p><div className="mt-4 flex flex-wrap gap-2"><Btn primary onClick={exportBackup}><Download size={15}/>Export backup</Btn></div></Card>}
function QR({child,notify}:any){const [v,setV]=useState(window.location.origin);const src=`https://quickchart.io/qr?size=500&text=${encodeURIComponent(v)}`;return <Card title={`QR Codes · ${title(child)}`}><div className="grid gap-5 md:grid-cols-2"><div><Input label="URL or text" value={v} onChange={setV}/><div className="mt-3"><Btn primary onClick={async()=>{try{const r=await fetch(src);if(!r.ok)throw new Error("QR service unavailable");const b=await r.blob(),u=URL.createObjectURL(b),a=document.createElement("a");a.href=u;a.download=`wedding-${child}-qr.png`;a.click();URL.revokeObjectURL(u);notify("QR downloaded.")}catch(e){notify(e instanceof Error?e.message:"QR download failed")}}}><Download size={15}/>Download PNG</Btn></div></div><img src={src} alt="QR code" className="mx-auto h-64 w-64 max-w-full rounded-xl border p-2"/></div></Card>}
function Locations({token,events,notify,child}:any){const [e,setE]=useState<EventRecord|null>(events[0]||null),[d,setD]=useState<R>(events[0]||{});useEffect(()=>{if(e)setD({...e})},[e]);return <div className="grid gap-5 md:grid-cols-[280px_minmax(0,1fr)]"><Card title="Venues"><div className="space-y-2">{events.map(x=><button key={x.id} type="button" onClick={()=>setE(x)} className={`w-full rounded-xl border p-3 text-left ${e?.id===x.id?"border-primary bg-primary/5":""}`}>{x.title}</button>)}</div></Card><Card title={`Location · ${title(child)}`}><Input label="Venue" value={d.venue_name} onChange={v=>setD({...d,venue_name:v})}/><div className="mt-4"><Area label="Address" value={d.venue_address} onChange={v=>setD({...d,venue_address:v})}/></div><div className="mt-4"><Input label="Google Maps URL" value={d.maps_url} onChange={v=>setD({...d,maps_url:v})}/></div><div className="mt-4"><Btn primary disabled={!e} onClick={async()=>{if(!e)return;try{await supabaseRest("events",{method:"PATCH",token,query:`id=eq.${encodeURIComponent(e.id)}`,body:{venue_name:d.venue_name,venue_address:d.venue_address,maps_url:d.maps_url}});notify("Location saved to Draft.")}catch(err){notify(err instanceof Error?err.message:"Location save failed")}}}><Check size={15}/>Save Draft</Btn></div></Card></div>}
function Publishing({token,notify,child}:any){const [r,setR]=useState<any[]>([]);const load=async()=>{try{const x=await fetch("/api/admin-advanced?action=versions",{headers:{Authorization:`Bearer ${token}`}}),d=await x.json().catch(()=>({}));if(!x.ok||!d.ok)throw new Error(d.error||"Revision load failed");setR(d.versions||[])}catch(e){notify(e instanceof Error?e.message:"Revision load failed")}};useEffect(()=>{void load()},[token]);const rollback=async(id:string)=>{if(!confirm("Restore this version into Draft?"))return;try{const x=await fetch("/api/admin-advanced?action=rollback",{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({id})}),d=await x.json().catch(()=>({}));if(!x.ok||!d.ok)throw new Error(d.error||"Rollback failed");notify("Version restored to Draft. Live website unchanged.");await load()}catch(e){notify(e instanceof Error?e.message:"Rollback failed")}};return <Card title={`Publishing · ${title(child)}`} action={<Btn onClick={()=>void load()}><RefreshCw size={15}/>Refresh</Btn>}><div className="mb-4 rounded-2xl border border-dashed p-4 text-sm text-muted-foreground">Draft → Preview → Validation → Publish. Rollback only restores Draft.</div><div className="space-y-2">{r.map(x=><div key={x.id} className="flex flex-wrap items-center gap-3 rounded-xl border p-3"><div className="min-w-0 flex-1"><b>Version {x.version_no}</b><div className="break-words text-xs text-muted-foreground">{x.status} · {x.label||"Revision"} · {x.created_at?new Date(x.created_at).toLocaleString():""}</div></div>{x.status==="published"&&<Btn onClick={()=>void rollback(x.id)}><RefreshCw size={15}/>Restore to Draft</Btn>}</div>)}{!r.length&&<Empty text="No revisions found."/>}</div></Card>}
function Diagnostics({health,notify,child}:any){const [r,setR]=useState<any[]>([]);return <Card title={`Testing & Diagnostics · ${title(child)}`} action={<Btn primary onClick={()=>{setR(health.map(h=>({name:h.label,ok:h.status!=="error",detail:h.detail})));notify("Diagnostics completed.")}}><Activity size={15}/>Run checks</Btn>}><div className="mb-4 rounded-2xl border border-dashed p-4 text-sm text-muted-foreground">Run the current checks for the selected test area before publishing.</div>{r.length?r.map(x=><div key={x.name} className="mb-2 rounded-xl border p-3">{x.ok?"✓":"✕"} <b>{x.name}</b><div className="text-xs text-muted-foreground">{x.detail}</div></div>):<Empty text="Run checks to inspect this area."/>}</Card>}
function System({health,child}:any){const filtered=child==="health"||child==="diagnostics"?health:health.filter(h=>{const l=h.label.toLowerCase();return child==="database"?l.includes("data")||l.includes("supabase"):child==="supabase"?l.includes("supabase"):child==="google-drive"?l.includes("drive"):child==="vercel"?l.includes("vercel"):child==="api"?l.includes("session")||l.includes("api"):child==="environment"?l.includes("configuration"):child==="storage"?l.includes("storage"):true});return <Card title={`System · ${title(child)}`}><div className="space-y-2">{filtered.map(h=><div key={h.label} className="rounded-xl border p-3"><b>{h.label}</b><div className="break-words text-xs text-muted-foreground">{h.detail}</div></div>)}{!filtered.length&&<Empty text="No health data is available for this selection."/>}</div></Card>}
const descriptions:Record<string,string>={theme:"Select the wedding visual theme and overall presentation.",colors:"Configure the global color palette.",typography:"Configure font and text hierarchy.",backgrounds:"Configure page and section backgrounds.",buttons:"Configure button appearance and defaults.",animations:"Configure motion and transitions.",responsive:"Configure responsive behavior.","loading-screen":"Configure the single loading screen.","loading-text":"Edit the loading message.","blink-heart":"Enable or disable the blinking heart treatment.","loading-duration":"Set the loading duration.","intro-video":"Configure the optional intro video source.",autoplay:"Control intro autoplay.",mute:"Control the initial mute state.","skip-button":"Control the skip button.",fullscreen:"Control fullscreen behavior.",preview:"Preview the saved intro configuration.",library:"Manage the media library settings.",photos:"Configure photo handling and display.",videos:"Configure video handling and display.",folders:"Configure media folder organization.","google-drive":"Configure Google Drive media source settings.","event-media":"Configure event media association.","upload-queue":"Review upload queue behavior.","failed-uploads":"Review failed upload handling.",statistics:"Review media statistics settings.",favorites:"Configure favorite interactions.",reactions:"Configure reaction interactions.",likes:"Configure likes.",wishes:"Configure wishes.",sharing:"Configure sharing behavior.",settings:"Configure interaction settings.",instagram:"Configure Instagram link.",youtube:"Configure YouTube link/video.",facebook:"Configure Facebook link.",whatsapp:"Configure WhatsApp contact.",phone:"Configure phone contact.",email:"Configure email contact.","social-sharing":"Configure social sharing metadata.","venue-list":"Manage venue settings.","add-venue":"Configure new venue defaults.","edit-venue":"Configure venue editing defaults.","google-maps":"Configure Google Maps behavior.",directions:"Configure directions text/link.",contact:"Configure venue contact details.",website:"Configure public website settings.",general:"Configure general application settings.",wedding:"Configure wedding-specific settings.","date-time":"Configure wedding date/time and timezone.",language:"Configure language and formatting.",notifications:"Configure notification preferences.",advanced:"Configure advanced settings.","getting-started":"Start using the admin system.",documentation:"Admin documentation settings.","feature-guide":"Feature guidance and status.",troubleshooting:"Troubleshooting information.",shortcuts:"Keyboard shortcut information.","system-information":"System version and environment information.",about:"Application information."};
function SelectionWorkspace({module,selected,settings,setSettings,token,notify}:any){const key=`admin.panel.${module.id}.${selected}`;const saved=settings[key]&&typeof settings[key]==="object"?settings[key]:{};const [enabled,setEnabled]=useState(saved.enabled!==false),[value,setValue]=useState(saved.value??""),[notes,setNotes]=useState(saved.notes??"");useEffect(()=>{const x=settings[key]&&typeof settings[key]==="object"?settings[key]:{};setEnabled(x.enabled!==false);setValue(x.value??"");setNotes(x.notes??"")},[key,settings]);const isUrl=["instagram","youtube","facebook","whatsapp","phone","email","google-maps","directions","website","google-drive","intro-video"].includes(selected);const isDuration=["loading-duration"].includes(selected);const save=async()=>{try{const payload={enabled,value,notes,updatedAt:new Date().toISOString()};await saveSiteSetting(token,key,payload);setSettings({...settings,[key]:payload});notify(`${module.label} → ${title(selected)} saved to Draft.`)}catch(e){notify(e instanceof Error?e.message:"Unable to save selection")}};return <Card title={`${module.label} · ${title(selected)}`} action={<span className="text-xs text-muted-foreground">Selection is matched to this panel</span>}><div className="mb-5 rounded-2xl border border-dashed p-4 text-sm text-muted-foreground">{descriptions[selected]||`${module.label} · ${title(selected)} configuration.`}</div><div className="grid gap-4 md:grid-cols-2"><Toggle label={`Enable ${title(selected)}`} value={enabled} onChange={setEnabled}/><Input label={isDuration?"Duration (milliseconds)":isUrl?"URL / identifier":"Configuration value"} type={isDuration?"number":"text"} value={value} onChange={setValue} placeholder={isDuration?"Example: 2500":isUrl?"Enter the matching URL or ID":`Enter ${title(selected).toLowerCase()} value`}/><div className="md:col-span-2"><Area label="Notes / additional configuration" value={notes} onChange={setNotes}/></div></div><div className="mt-5 flex flex-wrap gap-2"><Btn primary onClick={()=>void save()}><Check size={15}/>Save Draft</Btn><Btn onClick={()=>{setEnabled(saved.enabled!==false);setValue(saved.value??"");setNotes(saved.notes??"")}}><RefreshCw size={15}/>Reset</Btn>{isUrl&&value&&<Btn onClick={()=>window.open(value,"_blank","noopener,noreferrer")}><ExternalLink size={15}/>Open</Btn>}</div></Card>}
