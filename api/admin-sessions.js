const SUPABASE_URL=(process.env.SUPABASE_URL||'').replace(/\/$/,'');const SERVICE=process.env.SUPABASE_SERVICE_ROLE_KEY||'';
function json(res,s,b){res.status(s).setHeader('Content-Type','application/json');res.end(JSON.stringify(b));}
async function f(path,opts={}){const r=await fetch(`${SUPABASE_URL}${path}`,{...opts,headers:{apikey:SERVICE,Authorization:`Bearer ${SERVICE}`,'Content-Type':'application/json',...(opts.headers||{})}});const t=await r.text();let d={};try{d=t?JSON.parse(t):{}}catch{}if(!r.ok)throw new Error(d.message||t||`Supabase ${r.status}`);return d;}
async function admin(token){const r=await fetch(`${SUPABASE_URL}/rest/v1/rpc/is_admin`,{method:'POST',headers:{apikey:SERVICE,Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:'{}'});return r.ok&&await r.json();}
export default async function handler(req,res){try{const token=String(req.headers.authorization||'').replace(/^Bearer\s+/i,'');if(!(await admin(token)))return json(res,401,{ok:false,error:'Admin authorization required.'});const action=req.query?.action||(req.body||{}).action||'list';if(action==='list')return json(res,200,{ok:true,sessions:await f('/rest/v1/admin_sessions?select=*&revoked_at=is.null&order=last_seen_at.desc&limit=100')});if(action==='check'){const checkId=String(req.query?.id||'');if(!checkId)return json(res,400,{ok:false,error:'Session ID required.'});const rows=await f(`/rest/v1/admin_sessions?id=eq.${encodeURIComponent(checkId)}&select=revoked_at,last_seen_at&limit=1`);return json(res,200,{ok:true,revoked:!!rows[0]?.revoked_at});}const id=String((req.body||{}).id||'');if(!id)return json(res,400,{ok:false,error:'Session ID required.'});if(action==='force_logout'){const rows=await f(`/rest/v1/admin_sessions?id=eq.${encodeURIComponent(id)}&select=user_id`);const uid=rows[0]?.user_id;if(uid){await f(`/auth/v1/admin/users/${encodeURIComponent(uid)}`,{method:'PUT',body:JSON.stringify({user_metadata:{force_logout_at:new Date().toISOString()}})});}await f(`/rest/v1/admin_sessions?id=eq.${encodeURIComponent(id)}`,{method:'PATCH',headers:{Prefer:'return=representation'},body:JSON.stringify({revoked_at:new Date().toISOString()})});return json(res,200,{ok:true});}return json(res,400,{ok:false,error:'Unknown action.'});}catch(e){return json(res,500,{ok:false,error:e.message||'Session operation failed.'});}}
